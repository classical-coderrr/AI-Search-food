
错误报告
========

记录日期：2026-09-11

* 菜谱详情页分区右上角缺少关闭按钮
- 问题现象：进入菜谱详情后，概览、准备食材、烹饪过程和更多信息四个分区的右上角没有关闭入口，原本显示的说明文字也占用了截图标注的位置。
- 原因：关闭逻辑只放在料理大厅外层窗口标题栏，详情分区标题栏没有独立的关闭控件；各分区标题栏右侧仍渲染说明文字。
- 解决方案：在四个分区标题栏右上角统一增加可访问的关闭按钮，绑定已有的 `closeDetailView`；删除四个标题栏右侧说明文字，并通过相对定位和固定触控尺寸保持按钮在右上角可见。
- 验证结果：前端详情源代码测试通过，前端 52 项测试通过，前端构建通过；本地页面可正常进入主厨料理大厅。

* 多种输入食材生成的单道菜谱混入过多食材，且没有优先返回 B 站已有教程
- 问题现象：输入多种食材时，单道生成结果可能同时使用第三种及以上输入食材；菜谱排序也没有根据 B 站现成视频进行优先级调整。
- 原因：原有多菜谱提示只要求拆分食材，没有为每道菜锁定具体搭配，也缺少后端对单道菜谱实际使用输入食材数量的校验；生成完成后未根据视频搜索结果重新排序。
- 解决方案：为多食材请求按规则为每道菜分配一对核心食材，在提示词中明确每道菜只能使用一至两种输入食材；后端校验每道菜不得包含第三种输入食材且必须包含指定搭配；生成完成后按 `videoKeywords` 或菜名检索 B 站，有匹配视频的菜谱优先展示，检索失败则保留 AI 原始顺序。
- 验证结果：后端完整测试 431 项通过，菜谱服务专项测试通过；前端视频优先级测试和完整前端测试通过，前端构建通过。

* 单食材生成的第二、第三道菜谱内容重复
- 问题现象：输入“螃蟹”时第二、第三道菜谱重复；输入“鸡蛋”时三道菜谱都可能返回相同内容。
- 原因：三道菜谱由后端连续独立请求生成，后续请求原先只收到“风格不同”的通用提示，没有读取前面已生成的菜名、简介、食材和做法，因此模型可能重复输出同一道菜。
- 解决方案：后续菜谱提示词加入前序菜谱的去重上下文，明确禁止复用菜名、简介、食材清单和步骤；服务端按菜名或完整内容指纹检测重复，发现重复时自动换一种做法重试一次，仍重复则拦截并提示重新生成；前端增加“正在更换重复做法”的状态提示。
- 验证结果：新增重复检测、前序提示和自动重试测试；后端完整测试 434 项通过，前端完整测试 53 项通过，前端构建通过。

管理员端 AI 协议配置迁移版本冲突
- 问题现象：后端 Spring Boot 测试上下文无法启动，Flyway 报告新增协议迁移与项目已有迁移使用了相同版本号。
- 原因：项目已有手机号验证相关的 V4、V5 迁移，新增协议字段迁移不能复用历史版本号；Flyway 不允许重复版本号。
- 解决方案：将新增迁移调整为当前历史末尾的 `V30__add_ai_model_protocol.sql`，保留已有迁移版本不变。
- 验证结果：干净重建后 Flyway 成功校验 30 个迁移，定向后端测试继续通过。

管理员端 AI 协议配置同步器无法被 Spring 实例化
- 问题现象：应用上下文启动失败，提示 `DotEnvSynchronizer` 没有默认构造器。
- 原因：同步器同时提供了生产环境的字符串路径构造器和测试用的 `Path` 构造器，Spring 无法自动判断注入入口。
- 解决方案：为配置注入用的字符串构造器增加 `@Autowired`，保留 `Path` 构造器供单元测试使用。
- 验证结果：修复后干净构建成功，Spring 上下文可以正常启动。

管理员端 AI 配置集成测试污染本地 `.env`
- 问题现象：集成测试调用保存配置接口时，将测试用配置写入了项目根目录的真实 `.env`。
- 原因：测试环境沿用了生产默认的 `.env` 同步路径，测试数据没有和本地配置文件隔离。
- 解决方案：将测试环境的同步路径改为 `backend/target/test-ai-config.env`，并清理本次测试产生的五行 `AI_TEXT_RECIPE_*` 配置，未输出或保留任何密钥值。
- 验证结果：根目录 `.env` 不再包含本次测试新增的 AI 配置项，后续集成测试只写入 `target` 下的临时文件。

Anthropic 协议回归测试样例 JSON 转义错误
- 问题现象：Anthropic 客户端测试执行时提示响应不是有效 JSON。
- 原因：Java 文本块中的内层菜谱 JSON 引号未按响应字符串再次转义，测试样例本身生成了非法 JSON。
- 解决方案：改为先构造合法的菜谱 JSON，再用 `ObjectMapper` 序列化 Anthropic `content` 响应，避免手写嵌套转义。
- 验证结果：Anthropic 普通请求和流式请求定向测试均通过。

管理后台新增协议字段后的表单跨列样式错位
- 问题现象：新增“接口协议”后，原表单按第 3、4 项配置的跨列规则会让模型名称和 API Key 的布局不符合预期。
- 原因：表单项目数量增加后，接口地址和 API Key 的序号从第 3、4 项变为第 4、5 项，旧 CSS 选择器没有同步调整。
- 解决方案：将桌面端和移动端的跨列选择器更新为第 4、5 项，保证接口地址与 API Key 的布局一致。
- 验证结果：已重新检查模板与响应式样式，并重新执行前端构建。

管理员端 AI 配置保存请求漏传接口协议
- 问题现象：管理后台可以切换 OpenAI 兼容和 Anthropic 兼容，但保存配置时后端收不到最新协议。
- 原因：前端测试连接请求包含 `protocol`，保存配置请求对象却遗漏了该字段。
- 解决方案：保存配置时同步提交当前接口协议；清空地址后切换服务商或协议时自动恢复对应默认地址。
- 验证结果：前端 55 项测试通过，生产构建通过，变更检查通过。

管理员端 AI 配置同步失败
- 问题现象：点击保存配置时报 `AI 配置已保存，但 .env 同步失败`，后端堆栈根因是 `/app/config/.env.tmp` 的 `AccessDeniedException`。
- 原因：Docker 容器中的 Java 进程以 `app` 用户运行，挂载的 `.env` 文件可写，但 `/app/config` 父目录由 `root` 管理且不可写；原同步器先在同目录创建临时文件，无法完成创建。
- 解决方案：改为直接截断并写入已有 `.env` 文件，绕过 bind mount 父目录的临时文件创建限制；保留数据库事务回滚和权限错误提示。
- 验证结果：容器内确认 `.env` 文件可写但父目录不可写；同步器单元测试、后端全量测试和构建检查通过，测试临时文件已清理。

实际菜谱联调等待窗口超时
- 问题现象：输入“鸡蛋”进行实际生成时，第三道菜谱生成期间浏览器自动化等待超过单次执行时限并重置了测试会话。
- 原因：真实 AI 流式生成需要等待多道菜谱依次返回，单次浏览器自动化等待时限短于完整生成耗时；不是前端或后端接口报错。
- 解决方案：重新连接原有测试页面，不重复提交请求，继续读取最终页面状态。
- 验证结果：页面最终显示 `3 / 3 道已完成`，返回西红柿炒鸡蛋、豆腐蒸鸡蛋、青菜蛋花汤，浏览器和后端错误日志均为空。

* qwen3 菜谱生成速度明显变慢
- 问题现象：本机切换到 qwen3.8-max 后，菜谱生成速度明显低于之前使用 qwen-plus 的体验。
- 原因：qwen3 系列默认可能启用思考过程，而菜谱接口原本没有关闭思考，也没有限制单次输出长度；生成三道菜时会额外等待较长的推理和输出内容。
- 解决方案：保留管理员端选择的模型，不强制替换为 qwen-plus；针对 qwen3 模型请求增加 `enable_thinking=false`，并将单道菜谱最大输出限制为 2400 tokens，同时保留 SSE 流式返回。
- 验证结果：qwen3 请求参数定向测试通过；本机 Docker 联调输入“鸡蛋”约 50 秒完成，页面显示 `3 / 3 道已完成` 且三道菜谱内容不同。

* 管理员端 AI 配置未传递到图像识别
- 问题现象：管理员端保存 AI 配置后，食材识别台仍可能调度失败，视觉请求没有稳定使用管理员端保存的地址和协议。
- 原因：视觉客户端此前直接使用环境默认地址，没有把管理员端保存的 Base URL 补全为 `/chat/completions`；同时请求体和响应解析只覆盖 OpenAI 兼容格式，无法完整适配 Anthropic 兼容配置。
- 解决方案：让视觉配置复用已保存的 Qwen 文本配置地址、密钥和协议；OpenAI Base URL 自动补全路径，Anthropic 使用对应请求头、图片内容结构和响应解析，并保留 `qwen-vl-plus` 视觉模型。
- 验证结果：本机通过食材识别接口上传食物图片成功返回“牛肉、青椒、红椒、姜”，返回模型为 `qwen-vl-plus`；视觉配置和客户端定向测试通过。

Docker 后端重建网络依赖失败
- 问题现象：第一次重建时 Ubuntu 软件源连续返回 502；绕过后，Docker 内 Maven 下载 `spring-webmvc` 时又出现文件传输提前结束。
- 原因：镜像构建依赖外部 Ubuntu/Maven 软件源的瞬时网络稳定性，且原 Dockerfile 每次构建都需要安装 `curl`。
- 解决方案：健康检查改用 BusyBox 提供的轻量 `wget`，移除 Ubuntu `apt-get` 依赖；Docker Maven 构建增加持久缓存，减少后续重复下载。
- 验证结果：后端镜像最终构建成功并启动为 healthy，前端和 MySQL 均保持 healthy。

新增 AI 请求测试样例 JSON 转义错误
- 问题现象：新增 qwen3 速度测试和图像识别测试第一次执行时报 JSON 解析错误。
- 原因：模拟接口响应中嵌套 JSON 字符串少了一层 Java 引号转义，测试样例本身不是合法响应。
- 解决方案：修正测试数据的双层引号转义。
- 验证结果：相关定向测试 14 项全部通过，后端完整测试 442 项全部通过。

定向测试命令参数解析失败
- 问题现象：第一次执行包含多个测试类的 Maven 定向测试时，PowerShell 报参数列表解析错误，测试未启动。
- 原因：包含逗号的 `-Dtest` 参数未整体加引号，被 PowerShell 按表达式解析。
- 解决方案：将 Maven 参数整体加引号后重新执行。
- 验证结果：定向测试随后成功执行，相关 14 项全部通过。

源码检查命令中的 PowerShell 变量展开错误
- 问题现象：读取三个 Java 源文件时，嵌套 PowerShell 命令中的文件路径变量被提前展开，导致命令提示找不到路径；代码和项目运行未受影响。
- 原因：外层 PowerShell 在调用内层命令前先解释了 `$p` 变量，内层 `Get-Content` 实际没有收到文件路径。
- 解决方案：改用带引号的直接文件路径读取，随后成功读取并检查相关源文件。
- 验证结果：核心服务测试 35 项全部通过，未发现源码读取或编码问题。

* B 站生成前检索被接口风控拦截
- 问题现象：用原有项目 User-Agent 访问 B 站视频搜索接口时返回 412，无法取得可信视频标题；若直接放行会破坏“无来源不生成”的安全约束。
- 原因：B 站接口对非浏览器请求头触发安全风控，原客户端只有项目自定义 User-Agent，没有来源页和 Origin 信息。
- 解决方案：改用常见浏览器请求头，并补充 B 站页面 Referer 与 Origin；保留原有 HTTPS、响应结构校验和失败闭环。
- 验证结果：同一食材检索接口已返回真实视频候选，客户端、生成前核验和失败闭环测试均通过；后端完整测试 446 项全部通过。

Docker 容器内 B 站请求仍被出口风控拦截
- 问题现象：宿主机直接访问 B 站搜索接口可以返回真实结果，但通过本机 Docker 部署生成菜谱时，生成前核验提示“未找到与当前食材匹配的可核验 B 站菜谱视频”。
- 原因：Docker 容器使用的出口网络被 B 站返回 412 风控响应，即使补齐浏览器请求头也无法取得视频结果。
- 解决方案：保留“未取得 B 站可信来源就不生成”的严格策略，避免模型在外部检索失败时胡编菜肴；要恢复 Docker 中的真实生成，需要放行容器出口或配置可访问 B 站的合规代理/上游服务。
- 验证结果：容器内请求明确返回 B 站 code=-412，宿主机请求返回 code=0；SSE 正确发送错误事件并停止生成，没有放行无来源菜谱。

Docker 健康检查工具与运行时 libc 不兼容
- 问题现象：尝试将 BusyBox 的 wget 复制到 JRE 运行镜像后，容器内执行该工具报 GLIBC_2.38 不满足，健康检查工具与运行时环境不兼容。
- 原因：BusyBox 构建阶段的二进制依赖比运行阶段 Ubuntu Jammy 提供的 libc 版本更新。
- 解决方案：移除跨镜像复制的 BusyBox wget，改用运行镜像自带的 `/usr/bin/curl` 执行健康检查，避免额外安装软件源依赖。
- 验证结果：后端镜像重新构建成功，后端、前端和 MySQL 容器均为 healthy。

本轮动态菜谱数量改造首次编译失败
- 问题现象：将菜谱数量改为动态后，后端编译提示流式服务仍引用已删除的固定数量常量。
- 原因：批次总数和列表容量的两处旧引用没有同步替换为动态数量。
- 解决方案：改为使用本次请求计算出的 3 至 5 道菜谱数量，并保留非法数量回退为 3 道。
- 验证结果：菜谱定向测试和后端全量测试均重新通过。

动态菜谱提示词回归断言未同步
- 问题现象：定向测试中原有 B 站提示词断言失败，测试期待“不得用模型记忆补写菜谱”原句。
- 原因：新增无视频降级规则时将原句改写成了更具体的表述，功能逻辑正确但破坏了已有文案契约。
- 解决方案：恢复原有关键约束原句，同时补充“不得返回空数据”和“不得虚构菜式/视频”的降级规则。
- 验证结果：后端菜谱定向测试 31 项、前端定向测试 56 项、后端全量测试 450 项全部通过。

五道菜谱真实接口冒烟测试超过命令时间窗口
- 问题现象：输入 9 种食材进行真实 SSE 冒烟时，接口已返回 `total=5` 并生成前两道菜谱，但测试命令在 20 秒上限后结束。
- 原因：五道菜谱按顺序调用 AI 生成，单次冒烟命令的 20 秒时间上限不足以等待完整批次完成。
- 解决方案：确认批次数量、两种食材组合约束和无 B 站视频降级均已在前置事件中生效；完整批次联调应使用更长的等待窗口。
- 验证结果：Docker 后端、前端和 MySQL 均 healthy；SSE 已确认返回 5 道批次信号且未因 B 站无结果返回空数据。

五道菜谱批次中辅料别名被误判为第三种输入食材
- 问题现象：真实输入“番茄、鸡蛋、牛肉、土豆、青椒、洋葱、胡萝卜、香菇、豆腐”时，实际菜谱 ingredients 使用了番茄、鸡蛋和常见辅料“葱”，校验却提示包含第三种输入食材“洋葱”，导致 SSE 中断。
- 原因：输入食材与生成食材的模糊匹配允许任意子串匹配，单字辅料“葱”被错误匹配成“洋葱”。
- 解决方案：收紧短名称的模糊匹配规则，避免单字辅料通过包含关系匹配多字输入食材；同时为食材约束失败增加专用重试提示，明确要求遵守固定两种核心食材。
- 验证结果：新增“葱不应匹配洋葱”回归测试通过；重新构建 Docker 后，9 种食材真实 SSE 返回 5 道菜谱并收到 complete 事件。

新增食材约束回归断言初次放错测试位置
- 问题现象：为验证食材约束重试提示新增断言时，第一次插入到了重复菜谱测试，导致定向测试断言失败。
- 原因：按相同的 `streamRecipe` 调用次数定位代码时，误匹配了前一个测试方法。
- 解决方案：将断言移动到“第三种输入食材触发重试”的测试，并捕获第二次生成提示进行校验。
- 验证结果：菜谱定向测试 33 项、后端全量测试 452 项均通过。

* 动态菜谱数量取消上限后残留旧常量引用
- 问题现象：本轮取消菜谱数量固定上限后，后端编译仍提示 `RecipeRecommendationService` 引用了已删除的 `MAX_RECOMMENDATION_COUNT`。
- 原因：动态数量改造只替换了批次数量计算，食材配对列表的安全数量仍保留旧的最大值限制。
- 解决方案：配对列表改为使用本次请求计算出的批次数量，仅保留至少 3 道的兜底，不再引用固定上限常量。
- 验证结果：菜谱定向测试 38 项通过；后端全量测试 457 项通过；Docker 后端镜像重新构建并保持 healthy。

* 输入猪肉被误判为未覆盖输入食材
- 日期：2026-09-11
- 问题现象：流式生成输入“猪肉”时，模型偶尔返回“五花肉、猪里脊肉、排骨”等明确猪肉部位，系统却提示“AI 返回的菜谱组合未覆盖输入食材（缺少：猪肉）”，并中断生成。
- 原因：食材覆盖校验只按文本相等或连续包含判断，未将明确的猪肉部位归一化为“猪肉”；B站检索不是直接报错来源，截图消息来自批量食材覆盖校验。
- 解决方案：增加严格的猪肉部位别名归一化，并要求模型对肉类返回明确名称；保留未限定肉类名称的“肉丝/肉片”不直接视为猪肉。
- 验证结果：猪肉实际流式接口返回 `stream complete`；定向测试 40 项通过；后端全量测试 459 项通过；后端 Docker 健康检查通过。

Docker 构建阶段 Maven Surefire 依赖握手失败
- 日期：2026-09-11
- 问题现象：Docker 构建后端镜像执行 `mvn -B -DskipTests package` 时，从 Maven Central 下载 `surefire-extensions-spi:3.2.5` 发生 `Remote host terminated the handshake`，导致镜像构建失败。
- 原因：生产镜像构建使用了包含 `test` 生命周期的 `package`，即使跳过测试执行仍会解析 Surefire 插件及其依赖；该次外部仓库 TLS 握手中断后构建无法继续。
- 解决方案：将 Dockerfile 构建命令改为只执行 `compile`、`jar:jar` 和 `spring-boot:repackage`，生产镜像不再加载 Surefire；宿主机仍保留完整 Maven 测试流程。
- 验证结果：本地 Spring Boot 打包成功；Docker 后端镜像重新构建成功；backend、MySQL 容器均为 healthy，后端 `/actuator/health` 返回 `UP`。

* 固定菜谱数量导致多食材覆盖校验失败
- 日期：2026-09-11
- 问题现象：输入“虾、罗非鱼”时，系统按固定批次数量生成菜谱，模型返回结果未覆盖其中一种或多种输入食材，最终提示“AI 返回的菜谱组合未覆盖输入食材（缺少：虾、罗非鱼）”。
- 原因：后端和前端都把批次数量兜底为至少 3 道；两种不适合合并的食材仍被反复作为同一组合候选，导致生成数量与可行搭配不一致。
- 解决方案：由 AI 规划阶段返回覆盖全部输入所需的必要菜谱数量；多食材批次按实际数量生成，两种不适合合并的食材分别生成，并让前端直接使用 SSE 返回的 `total`，不再强制补成 3 道。
- 验证结果：输入“虾、罗非鱼”的真实 SSE 返回 `total: 2`，生成“香辣虾”和“清蒸罗非鱼”且两种输入均被覆盖；后端全量测试 464 项、前端测试 11 项和前端生产构建均通过。

* 菜谱数量规则曾被错误降到三道以下
- 日期：2026-09-12
- 问题现象：上一轮为解决多食材覆盖问题，将“虾、罗非鱼”等两种食材的菜谱数量缩减为 2 道，不符合最终明确的“至少 3 道，超过 3 种食材时菜谱数等于输入食材数”规则。
- 原因：把“生成数量不要固定”错误理解成规划模型可以缩短批次，且流式服务直接用规划结果数量覆盖了业务要求的总数。
- 解决方案：统一按 `max(3, 去重后的输入食材数)` 计算批次数量，规划阶段不得缩短数量；兜底候选让每种输入食材先独立占一道，只有短思考规划确认是大众常见搭配时才合并，库存不适配时直接忽略；最终仍用关闭思考的 SSE 逐道生成。
- 验证结果：数量、库存兼容、规划不足不缩量、B 站优先和无视频家常菜回退测试均通过；后端全量测试 466 项、前端测试 56 项、前端生产构建全部通过。

B 站家常菜回退文案旧断言导致完整测试失败
- 日期：2026-09-12
- 问题现象：首次完整后端测试有 1 项失败，旧断言仍要求提示词包含“不得用模型记忆补写菜谱”。
- 原因：该旧文案与本次明确保留的“没有 B 站视频时回退到常见家常菜并返回完整数据”规则冲突，业务提示词已更新但测试契约未同步。
- 解决方案：将提示词和测试统一为“可按常见家常菜知识补全完整做法，但不得虚构菜式、视频或来源”。
- 验证结果：定向测试 39 项通过，随后后端全量测试 466 项全部通过。

前端加载动画修改时补丁命令初次失败
- 日期：2026-09-12
- 问题现象：第一次应用 HomeView 加载动画补丁时，补丁工具提示存在空的 Update file hunk，文件未被修改。
- 原因：补丁文本末尾误包含了一个没有内容的 Update file hunk。
- 解决方案：删除空 hunk 后重新应用，成功写入加载模板、样式和前端用例。
- 验证结果：前端测试 57 项全部通过，前端生产构建成功。

本轮源码读取命令引用错误
- 日期：2026-09-12
- 问题现象：读取 HomeView 指定行时，第一次命令发生 PowerShell 引号解析错误，后续使用变量行号时被外层 PowerShell 展开，出现数组类型解析错误。
- 原因：嵌套 PowerShell 命令的引号和变量作用域处理不正确。
- 解决方案：改用 Select-Object 的 Skip/First 直接读取指定范围，完成相关代码检查。
- 验证结果：相关模板和样式已成功读取并完成修改，前端测试 57 项和生产构建均通过。

详情页库存提示条与标题区域视觉遮挡
- 日期：2026-09-12
- 问题现象：菜谱生成完成后打开详情页，“已按原输入食材生成菜谱。”提示条与“准备食材”标题及返回按钮发生重叠。
- 原因：详情页结果面板使用固定三行 CSS Grid，同时包含推荐选择器、可选提示条、详情标题和结果内容等多个动态子项；提示条出现时被分配到可压缩行，导致后续标题区域发生视觉挤压。
- 解决方案：详情页结果面板改为纵向 Flex 布局，让可选提示条和详情标题按实际高度顺序排列；结果内容使用剩余空间并继续保留内部滚动。
- 验证结果：新增详情页布局回归用例通过；前端测试 58 项全部通过，前端生产构建成功。

详情页布局补丁第一次匹配失败
- 日期：2026-09-12
- 问题现象：第一次应用详情页布局补丁时，测试文件中的中文断言与预期文本编码显示不一致，补丁工具未找到匹配段落，文件未被该次补丁修改。
- 原因：补丁上下文依赖了终端显示的乱码文本，而不是文件中的实际 UTF-8 内容。
- 解决方案：改用稳定的英文测试名称和 CSS 选择器作为补丁上下文，重新应用成功。
- 验证结果：详情页布局修复及回归用例均已写入，前端测试 58 项和生产构建均通过。

品牌区优化时图像生成技能路径首次读取失败
- 日期：2026-09-12
- 问题现象：首次按技能表读取 imagegen 规范时使用了不存在的路径，系统提示找不到 SKILL.md。
- 原因：技能表中的 r2 根目录包含 .system 子目录，首次读取时漏写了该层级。
- 解决方案：改用实际的 `.system/imagegen/SKILL.md` 路径重新读取，随后正常完成图标生成。
- 验证结果：像素菜谱书图标已生成并成功复制到前端 public/images 目录，前端测试和构建通过。

品牌区优化测试补丁首次匹配失败
- 日期：2026-09-12
- 问题现象：前两次应用 App 品牌区和回归测试补丁时，补丁工具未找到测试文件中包含中文断言的上下文，文件未被失败补丁修改。
- 原因：补丁上下文依赖了终端显示的中文编码内容，实际文件为 UTF-8 文本，显示结果与补丁上下文不一致。
- 解决方案：先读取文件的 UTF-8 实际内容，再改用稳定的英文选择器、类名和测试名称作为上下文，最终成功写入。
- 验证结果：品牌区改动及回归用例已成功写入，前端测试 58 项全部通过，生产构建成功。

品牌区预览时浏览器临时绑定变量未声明
- 日期：2026-09-12
- 问题现象：首次打开本地品牌区预览后尝试截图时，浏览器工具提示临时 tab 变量未定义。
- 原因：创建浏览器标签页时使用了未声明的变量赋值，标签页虽已打开，但绑定未保存。
- 解决方案：重新使用 let 声明并绑定预览标签页，完成品牌区截图检查。
- 验证结果：预览页面实际显示“小厨灵”和英文副标题，检查结束后临时开发服务已停止且 5173 端口已释放。

品牌区检索命令引用了不存在的项目根目录 package.json
- 日期：2026-09-12
- 问题现象：一次检索命令同时读取项目根目录 package.json 时提示文件不存在。
- 原因：package.json 位于 frontend 目录，项目根目录没有该文件。
- 解决方案：改用 frontend 目录及明确的源码路径进行检索，完成相关文件检查。
- 验证结果：品牌区源码、测试和素材均已确认，前端测试及生产构建成功。

服务器部署配置读取命令不兼容
- 日期：2026-09-12
- 问题现象：检查部署文件时，带通配符的 Compose 文件参数被当前终端拒绝，PowerShell 的 `Get-Content` 也未能通过 `rtk` 命令包装器解析，部署尚未开始。
- 原因：命令参数直接把 PowerShell 通配符传给了 Windows 路径解析器，且当前环境的 `rtk` 包装器不能直接解析该 PowerShell 内置命令。
- 解决方案：改用 `rtk rg` 的明确文件路径检索和 Node.js 文件读取方式继续检查部署配置，避免修改项目文件和泄露环境变量。
- 验证结果：已确认项目采用腾讯云 TCR 镜像部署方案，但仓库中未发现可直接使用的服务器 SSH 地址或登录凭据；需要补充服务器连接信息后才能执行远程更新。

Docker 构建阶段 Docker Hub 授权接口暂时 EOF
- 日期：2026-09-10
- 问题现象：第一次使用 Docker 构建后端镜像时，Dockerfile 语法镜像授权请求返回 EOF，构建未能开始。
- 原因：Docker Desktop 访问 Docker Hub 授权接口时出现瞬时网络中断。
- 解决方案：保留本地构建配置，稍后在不强制重新拉取基础镜像的情况下重试构建。
- 验证结果：后端镜像随后构建成功，前端镜像也构建成功并推送到 TCR。

生产 Compose 镜像标签未跟随 IMAGE_TAG 更新
- 日期：2026-09-12
- 问题现象：服务器 `.env.prod` 已设置新版本标签，但 `docker compose pull` 仍拉取前后端旧标签。
- 原因：`docker-compose.prod.yml` 的 backend 和 frontend image 行写死了旧版本，未引用 `${IMAGE_TAG}`。
- 解决方案：将生产 Compose 文件中的固定旧标签替换为本次 TCR 标签，或改为引用 `${TCR_REGISTRY}`、`${TCR_NAMESPACE}`、镜像名称和 `${IMAGE_TAG}` 变量；同时使用基础 Compose 文件和生产覆盖文件一起执行命令。
- 验证结果：已通过服务器文件检查定位根因并整理修复步骤；服务器完成新的 pull/up 后需再次确认容器标签和健康状态。

本轮组件源码读取命令变量被外层 PowerShell 提前展开
- 日期：2026-09-12
- 问题现象：首次分段读取小厨灵和场景窗口组件时，命令中的路径变量被清空，导致 `Get-Content` 缺少路径参数并读取失败。
- 原因：嵌套 PowerShell 命令中的变量被外层命令提前解析，传入子命令后只剩赋值符号和空路径。
- 解决方案：取消嵌套路径变量，改用明确的文件路径并指定 UTF-8 编码读取。
- 验证结果：小厨灵窗口、场景窗口、料理大厅嵌入样式和滚动容器源码均已成功读取并完成定位。

小厨灵窗口放大函数命名冲突
- 日期：2026-09-12
- 问题现象：首次执行前端生产构建时，Vue 编译提示 `Identifier 'toggleExpanded' has already been declared`。
- 原因：组件原有消息卡片展开函数与新增的小厨灵窗口放大函数使用了同名标识符。
- 解决方案：将窗口操作函数重命名为 `togglePanelExpanded`，并同步更新模板和源码测试断言。
- 验证结果：聚焦测试全部通过，前端生产构建成功。

前端完整测试发现管理员导航断言与现状不一致
- 日期：2026-09-12
- 问题现象：完整前端测试共 154 项，其中 153 项通过；`hotIngredientNavigation.test.js` 的管理员侧边栏顺序断言失败，实际导航包含 `feature-suggestions`，旧断言未包含。
- 原因：该测试断言落后于当前管理员导航注册表，且与本次小厨灵窗口和滚动优化无关。
- 解决方案：本次不修改无关功能，仅保留失败证据并隔离影响，待管理员导航功能后续一并同步测试。
- 验证结果：项目既有 `npm test` 58/58 通过，本次聚焦测试 32/32 通过，生产构建成功；完整测试的唯一失败项仍为上述无关断言。

小厨灵 Agent 定向测试首次编译缺少 Mockito 静态导入
- 日期：2026-09-12
- 问题现象：首次运行小厨灵 Agent 定向 Maven 测试时，新增测试编译提示找不到 `when(...)` 方法。
- 原因：测试新增 Mockito `when` 调用，但遗漏了对应的静态导入。
- 解决方案：补充 `org.mockito.Mockito.when` 静态导入。
- 验证结果：`QwenAgentClientTest` 6/6 通过，完整后端测试 468/468 通过。

小厨灵 Agent 将文本工具调用标记直接展示给用户
- 日期：2026-09-12
- 问题现象：用户请求根据番茄、茄子、鸡肉、牛肉和螃蟹生成晚餐时，界面显示了 `<tool_call>` 标签及其中的 JSON 参数，菜谱工具没有正常执行。
- 原因：模型将工具调用放在普通回答的 `content` 中，以 `<tool_call>...</tool_call>` 文本格式返回；Agent 解析器此前只识别标准 `tool_calls` 字段。
- 解决方案：增加文本工具调用标记解析，校验工具名称和 JSON 对象参数，生成缺失的调用编号，并从最终回答中移除协议标记；同时保留标准 OpenAI 和 Anthropic 工具调用解析。
- 验证结果：新增回归测试通过，完整后端测试 469/469 通过，前端测试 58/58 通过，生产构建成功。

小厨灵 Agent 将用户问题误当成菜谱食材
- 日期：2026-09-12
- 问题现象：用户询问“今晚能做什么”时，Agent 生成菜谱失败，并提示缺少食材“今晚能做什么”。
- 原因：模型工具调用返回了 `ingredients` 数组，但 Agent 只读取 `request` 字段，缺少字段时回退到整句用户问题；工具 schema 也没有声明该数组格式。
- 解决方案：优先读取并拼接 `ingredients` 数组，兼容原有 `request` 文本字段，并同步补充菜谱生成工具参数 schema。
- 验证结果：新增 Agent 参数回归测试通过，定向测试 13/13 通过，完整后端测试 471/471 通过。

Agent 现状检查命令与当前 Windows 环境不兼容
- 日期：2026-09-12
- 问题现象：首次批量检索 Agent Java 文件时，Windows 未展开 `*.java` 通配符，`rg` 报路径语法错误；随后尝试使用 `bat` 分段读取文件时，环境提示未安装该程序。
- 原因：当前 PowerShell/Windows 路径处理方式不会按预期展开传给 `rg` 的目录通配符，并且本机 PATH 中没有 `bat`。
- 解决方案：改为对明确文件路径执行 `rtk rg`，使用带 UTF-8 输出设置的 PowerShell 读取需要的文本范围，不再依赖目录通配符或 `bat`。
- 验证结果：已成功读取 Agent 服务、工具注册、模型客户端、前端组件及测试现状，完成本次只读架构评估。

前端生产构建首次执行目录错误
- 日期：2026-09-12
- 问题现象：首次执行前端生产构建时，项目根目录未找到 `package.json`，命令返回 ENOENT。
- 原因：前端工程位于 `frontend` 子目录，构建命令误在项目根目录执行。
- 解决方案：切换到 `D:\AI-Search-food\frontend` 目录重新执行构建。
- 验证结果：前端生产构建成功，Vite 完成 4837 个模块转换。

随机菜单独立生成首次测试缺少兼容构造函数
- 日期：2026-09-13
- 问题现象：为周菜单服务增加视频检索依赖后，定向 Maven 测试在测试编译阶段提示 `WeeklyMenuService` 找不到原有参数数量的构造函数。
- 原因：服务新增了可选的 `VideoSearchService` 构造参数，但原有单元测试仍使用旧构造方式。
- 解决方案：补充不带视频检索参数的兼容构造函数，并让它默认关闭外部检索。
- 验证结果：兼容构造函数已补回，后续将继续执行周菜单定向测试和完整测试。

随机菜单独立生成首次运行缺少生成记录回读桩
- 日期：2026-09-13
- 问题现象：独立随机菜单单元测试首次运行时，菜单保存校验返回 404“所选菜谱不存在或无权使用”。
- 原因：测试桩只模拟了新菜谱记录插入，没有让查询回读刚生成的菜谱记录。
- 解决方案：补充生成记录的回读桩，并为生成记录分配测试 ID。
- 验证结果：周菜单、模型客户端和控制器定向测试 30/30 通过，完整后端测试报告无失败项。

* AI 安排本周菜谱请求超时与生成耗时过长
- 日期：2026-09-13
- 问题现象：点击 AI 安排本周菜谱后，请求在生成完整周菜单前超时；延长超时后，随机生成一周菜单仍然等待较久。
- 原因：前端统一 Axios 请求超时为 30 秒；随机安排还依次检索早餐、午餐、晚餐推荐，并让模型以 2048 思考预算和 12000 最大输出量生成 21 个带扩展字段的餐次。
- 解决方案：将周菜单自动安排接口、开发代理和 Qwen 菜谱客户端读取超时调整为 180 秒；三类餐次推荐改为并行检索，思考预算降为 768，最大输出量降为 7000，并只要求菜名、简介和食材。
- 验证结果：后端定向测试 24/24、完整测试 473/473、前端测试 58/58 均通过，前端生产构建成功。

* 前端完整测试再次发现管理员导航断言滞后
- 日期：2026-09-13
- 问题现象：全量前端测试首次执行共 33 个测试文件、157 项测试，其中 156 项通过；`hotIngredientNavigation.test.js` 的管理员侧边栏顺序断言失败，实际结果包含 `feature-suggestions`，旧断言未包含。
- 原因：该断言与当前管理员导航注册表不同步，属于测试用例过期，不是运行时功能故障。
- 解决方案：同步测试期望顺序，补充 `feature-suggestions`。
- 验证结果：全量前端测试复跑 33/33 个文件、157/157 项通过。

前端测试脚本检查命令的 PowerShell 引号错误
- 日期：2026-09-13
- 问题现象：检查 `package.json` 测试脚本时，PowerShell 将嵌套双引号解析为命令，提示找不到 `test`。
- 原因：命令行字符串同时使用了 PowerShell 双引号和检索表达式双引号，转义方式不兼容当前 shell。
- 解决方案：改用 Node 读取 JSON 并动态收集测试文件，不再依赖易歧义的嵌套引号。
- 验证结果：全量前端测试及生产构建均成功。

Agent 控制器测试首次鉴权上下文与异步断言不匹配
- 日期：2026-09-13
- 问题现象：新增 Agent 控制器切片测试首次执行时，删除会话用例拿到空的 `AuthPrincipal`，流式入口的 `SseEmitter` 也未在切片环境报告异步已启动，3 项中 2 项失败。
- 原因：关闭过滤器的 `@WebMvcTest` 不会自动把请求后处理器中的认证对象放入当前线程安全上下文；切片测试对 `SseEmitter` 的异步生命周期做了超出该测试层级的断言。
- 解决方案：在测试前显式设置并在测试后清理 `SecurityContextHolder`，流式入口改为校验 HTTP 映射和服务调用，不断言切片容器的异步生命周期。
- 验证结果：Agent 控制器定向测试 3/3 通过。

前端 npm test 脚本遗漏部分测试文件
- 日期：2026-09-13
- 问题现象：原 `npm test` 只执行 9 个指定测试文件、58 项测试，未覆盖仓库中其余 24 个测试文件。
- 原因：测试脚本使用硬编码文件清单，新增测试不会自动纳入回归。
- 解决方案：改为使用 Node 测试运行器的自动发现模式 `node --test`。
- 验证结果：`npm test` 已自动执行 33 个测试文件、157 项测试，全部通过。

前端样式检索命令首次将 CSS 变量误识别为参数
- 日期：2026-09-13
- 问题现象：检索 `--app-accent` 相关样式时，`rg` 将以双短横线开头的表达式当作命令选项，提示参数无法识别。
- 原因：命令未使用 `--` 结束选项解析。
- 解决方案：在检索表达式前加入 `rg --`，按字面匹配 CSS 变量。
- 验证结果：已正常读取项目的主题色变量并完成黄色动画实现。

前端动画定位命令首次正则转义错误
- 日期：2026-09-13
- 问题现象：定位新增动画代码时，PowerShell 对包含引号的复合正则转义不完整，`rg` 报正则括号未闭合。
- 原因：命令字符串中的双引号被 shell 先行解析，导致传给 `rg` 的表达式被截断。
- 解决方案：改用不含嵌套引号的正则，并拆分检索条件。
- 验证结果：已确认动画模板、关键帧和自动发现测试脚本的实际行号。

文档审阅命令中的 PowerShell 变量被外层提前展开
- 日期：2026-09-14
- 问题现象：分段读取文档审阅规则时，内层 `$p` 和 `$lines` 变量被外层 PowerShell 提前展开，命令因缺少变量值而解析失败。
- 原因：嵌套 PowerShell 命令中的美元符号未转义。
- 解决方案：使用反引号转义内层变量后重新执行分段读取。
- 验证结果：文档审阅规则已完整读取，后续 DOCX 内容提取未受影响。

DOCX 渲染缺少捆绑的 LibreOffice
- 日期：2026-09-14
- 问题现象：使用文档审阅工具渲染开题报告时提示找不到 `soffice.exe`，无法生成页面图片。
- 原因：当前工作区依赖包包含文档处理 Python 环境和 Poppler，但未提供 LibreOffice 可执行文件，渲染器在 PATH 中也无法定位它。
- 解决方案：遵循文档审阅规则，不调用用户安装的 LibreOffice；改为完整提取 DOCX 表格内的正文与参考文献，按章节和原句定位引用问题，并明确说明页码未通过渲染确认。
- 验证结果：已成功提取正文 58 个段落，完成正文引用编号与 16 条参考文献的对应检查；原 DOCX 未被修改。

小厨灵 Redis 状态实现首次编译缺少 AgentStep 导入
- 日期：2026-09-14
- 问题现象：首次执行后端编译时，`AgentService` 在落盘工具步骤处提示找不到 `AgentStep` 类型，编译失败。
- 原因：新增步骤持久化逻辑后忘记在 `AgentService` 导入 `AgentStep`。
- 解决方案：补充 `com.example.food.agent.state.AgentStep` 导入。
- 验证结果：重新编译成功；新增定向测试 5/5 通过，完整后端测试 478/478 通过，Docker backend 镜像构建并启动成功。

测试上下文默认恢复扫描连接不到 Redis
- 日期：2026-09-14
- 问题现象：完整后端测试中，未使用测试 profile 的 Spring 上下文按默认配置启动 Agent 恢复扫描，日志出现连接 `localhost:6379` 被拒绝；测试结果仍为通过。
- 原因：普通开发配置将 `AGENT_RECOVERY_ENABLED` 默认设为开启，但这些测试没有启动 Redis。
- 解决方案：将非 Docker 的 `application.yml` 默认值改为关闭；Docker Compose 继续通过环境变量显式开启，测试 profile 也保持关闭。
- 验证结果：Redis 容器健康运行，Docker backend 健康检查通过；后续编译和测试验证无功能失败。

Docker Compose 未透传 Agent 恢复参数
- 日期：2026-09-14
- 问题现象：准备通过临时缩短 `AGENT_RECOVERY_STALE_AFTER` 做服务重启恢复演练时，发现 Compose 只传递了恢复开关，扫描间隔、过期阈值和租约时长不会进入 backend 容器。
- 原因：Docker Compose 的 `.env` 变量只有被显式写入 service environment 才会传递给容器；`application-docker.yml` 中的占位符无法直接读取宿主机 Compose 变量。
- 解决方案：在 backend 环境中补齐 Redis 超时、恢复扫描间隔、过期阈值和租约时长，并同步更新 `.env.example` 与 `.env.prod.example`。
- 验证结果：重新校验 Compose 配置并重建 backend 后，恢复参数可通过 Compose 环境覆盖；Redis 与 backend 健康检查继续通过。

Redis 状态持久化后小厨灵聊天窗口历史未恢复
- 日期：2026-09-14
- 问题现象：用户重启运行环境后重新打开小厨灵，之前的聊天气泡历史为空。
- 原因：Redis 当前只保存 AgentRun、checkpoint 和执行步骤；前端 `KitchenAgentWidget` 启动时只从浏览器 `localStorage` 恢复消息，后端虽然将消息写入 MySQL，但尚未提供历史消息查询接口。并且前端本地 key 依赖 JWT 后缀，重新登录或浏览器存储被重置后也会读到另一份空记录。
- 解决方案：保留 Redis 作为运行态与恢复态存储，新增基于用户权限的会话/消息查询接口；前端启动时优先从服务端恢复最近会话，localStorage 只作为离线缓存，并将历史归属改为稳定的用户标识而不是 token 片段。
- 验证结果：已通过代码审查确认该缺口；当前 Redis 中的 AgentRun 和 checkpoint 仍可读取，问题集中在聊天历史的服务端回读与前端初始化。

客户端历史恢复功能首次编译方法命名冲突
- 日期：2026-09-14
- 问题现象：新增会话历史查询方法后，后端编译提示 `AgentService` 中存在同签名的 `conversationHistory(Long, Long)` 方法，返回类型无法区分重载。
- 原因：原有方法用于构造模型上下文消息列表，新方法也使用了相同方法名返回历史响应对象；Java 不能仅按返回类型重载方法。
- 解决方案：将面向客户端的查询方法重命名为 `conversationHistoryDetails`，保留原有模型上下文方法不变。
- 验证结果：后端定向测试 6/6 通过，完整后端测试 479/479 通过。

客户端历史恢复定向测试命令的 PowerShell 逗号解析错误
- 日期：2026-09-14
- 问题现象：首次运行 Maven 定向测试时，PowerShell 将 `-Dtest=AgentControllerTest,InMemoryAgentRunStoreTest` 中的逗号表达式错误解析，命令未进入 Maven。
- 原因：PowerShell 外层参数没有将包含逗号的 Maven 参数整体引用。
- 解决方案：将测试选择参数改为带引号的 `"-Dtest=AgentControllerTest,InMemoryAgentRunStoreTest"`。
- 验证结果：定向测试正常执行并通过 6/6。

小厨灵 Agent 确认状态字段变更后的测试编译错误
- 日期：2026-09-14
- 问题现象：调整 `AgentConfirmationMapper.markConfirmed` 以持久化执行结果后，`AgentWriteServiceTest` 仍调用旧的两个参数方法，测试编译失败并提示参数数量不匹配。
- 原因：确认记录新增结果消息字段，Mapper 方法签名同步扩展，但测试桩和验证未同步更新。
- 解决方案：为测试桩、验证补充结果消息参数，并覆盖保存菜谱和通用厨房写操作两条路径。
- 验证结果：Agent 定向测试通过 12/12。

Agent 确认执行状态迁移首次不兼容 H2
- 日期：2026-09-14
- 问题现象：首次执行全量后端回归时，Flyway 迁移 V32 失败，导致 129 个 Spring 测试上下文错误；H2 拒绝在一个 `ALTER TABLE` 中连续添加多个字段。
- 原因：迁移脚本采用了 MySQL 可接受但 H2 不支持的多列 `ADD COLUMN` 语法。
- 解决方案：将 `processing_at`、`result_message`、`error_code`、`error_message` 拆成四条独立的 `ALTER TABLE` 语句。
- 验证结果：修复后全量后端测试通过 483/483，Flyway 成功应用 V32。

Docker Desktop 关机后 Linux 引擎未恢复
- 日期：2026-09-14
- 问题现象：关机重启后执行 Docker Compose 状态检查时，Docker CLI 无法连接 `dockerDesktopLinuxEngine`，提示 Linux 引擎管道不存在；手动启动 Docker Desktop 进程后等待 45 秒仍未就绪。
- 原因：Docker Desktop 的 Linux 引擎尚未完成启动，当前不是项目代码或测试失败。
- 解决方案：已启动 Docker Desktop 并保留进程；待引擎就绪后重新执行 Docker Compose 和 Redis/MySQL 运行验证，必要时手动重启 Docker Desktop 或 WSL。
- 验证结果：本次暂未完成容器运行态验证；不影响已通过的后端 484/484 和前端 158/158 测试。

* “保存这道菜”未触发保存菜谱工具
- 日期：2026-09-15
- 问题现象：用户在生成牛肉粿条汤后请求保存，Agent 回复“当前会话里没有可用的保存菜谱功能”，没有返回 `confirmation.required` 确认事件。
- 原因：后端虽然已注册 `recipe.save`/`recipe_save` 工具，但 `AgentToolRegistry.functionDefinitions` 只在原始消息包含“菜谱”“食谱”“收藏”等关键词时暴露该工具；“保存这道菜”只命中兜底分支，未加入 `RECIPE_SAVE`，模型因此无法调用保存工具。
- 解决方案：在工具筛选器中增加保存动词与菜谱指代组合判断，覆盖“保存这道菜”“把它收藏起来”等表达，并明确排除“保存本周菜单”等菜单操作误匹配。
- 验证结果：新增路由回归测试；定向 Agent 测试 18/18、后端全量测试 485/485 通过。

Apifox 调用 Agent 接口返回 401 未授权
- 日期：2026-09-15
- 问题现象：调用 Agent 接口时返回 `{"code":401,"message":"Unauthorized","data":null}`，请求未进入 Agent 业务逻辑。
- 原因：当前请求未通过 JWT 认证，常见原因是缺少 `Authorization: Bearer <JWT>`、令牌已过期，或使用了非普通用户会话令牌；仅携带 `X-Anonymous-Id` 不能访问 Agent 受保护接口。
- 解决方案：待使用当前登录普通用户重新获取完整 JWT，并确认 Apifox 实际请求头已发送 `Authorization` 后重试。
- 验证结果：根据后端 JWT 过滤器和认证入口判断为认证层问题，业务原因尚未进入验证。

目标提交隔离副本首次前端测试缺少依赖
- 日期：2026-09-15
- 问题现象：在不带依赖目录的目标提交隔离副本中首次运行前端测试时，2 个测试因找不到 `pinia` 和 `lucide-vue-next` 模块而失败，137 个测试通过。
- 原因：隔离副本未携带 `node_modules`，属于验证环境不完整，不是代码断言失败。
- 解决方案：接入现有前端依赖后重新运行测试。
- 验证结果：前端测试 158/158 通过，生产构建通过。

Agent 步骤审计测试首次编译参数数量不匹配
- 日期：2026-09-15
- 问题现象：新增 AgentService 集成测试时，Maven 测试编译提示 AgentService 构造函数需要 21 个参数，但测试只传入了 20 个参数。
- 原因：AgentService 新增 AgentIntentAuditService 依赖后，测试构造调用漏传了一个占位依赖参数。
- 解决方案：核对构造函数参数顺序，在测试中补齐缺少的空依赖参数并清理未使用导入。
- 验证结果：AgentIntentAuditServiceTest、AgentIntentRecognizerTest 和 AgentServiceTest 共 10 个测试全部通过。

写操作幂等账本测试首次出现 Mockito 无用桩错误
- 日期：2026-09-15
- 问题现象：新增 AgentWriteOperationService 依赖后，AgentWriteServiceTest 中部分不经过写操作分支的测试因通用 `operationService.claim` 测试桩未被调用而失败，5 个测试报 `UnnecessaryStubbingException`。
- 原因：测试初始化对所有用例统一设置了幂等账本桩，但状态查询、处理中和已完成分支会在账本调用前直接返回。
- 解决方案：将该通用桩标记为 lenient，保留成功写操作用例对账本调用的显式校验。
- 验证结果：已修复，待本轮定向测试再次确认。

写操作重复执行测试首次未 stub 幂等账本回放结果
- 日期：2026-09-15
- 问题现象：新增“已有幂等账本记录时不再执行业务写入”测试时，测试返回结果为 null，触发 NullPointerException。
- 原因：`AgentWriteOperationService` 在生产环境会生成回放结果，但测试中使用了 Mockito mock，未设置 `replay` 方法的返回值。
- 解决方案：为已完成账本记录补充 `replay` 返回值桩，继续校验业务写服务不会再次调用。
- 验证结果：已修复，待本轮定向测试再次确认。

* 写操作幂等账本测试问题复验闭环
- 日期：2026-09-15
- 问题现象：前述幂等账本测试的无用桩和回放结果桩问题需要在事务边界调整后再次确认。
- 原因：测试用例曾分别覆盖到 Mockito 严格桩校验和未 stub 回放结果两个失败点。
- 解决方案：保留 lenient 通用桩，并为重复幂等键用例补齐已完成结果回放桩。
- 验证结果：定向测试 30/30、后端全量测试 501/501、前端测试 158/158 均通过；前端生产构建和 Docker 服务健康检查通过。

* 幂等账本测试闭环数量更新
- 日期：2026-09-15
- 问题现象：在复验闭环后又补充了写入失败状态覆盖，需要更新本轮最终测试数量。
- 原因：新增失败状态测试后，幂等键相关定向用例由 30 个增加到 32 个。
- 解决方案：保留失败状态持久化断言，并重新执行后端全量测试。
- 验证结果：幂等键相关定向测试 32/32、后端全量测试 503/503 通过；前端测试 158/158，前端生产构建通过。

客户端未登录时的接口检查命令首次读取响应失败
- 日期：2026-09-15
- 问题现象：使用 PowerShell `Invoke-WebRequest` 检查未登录的写操作查询接口时，命令处理错误响应失败，提示 `HttpResponseMessage` 不支持 `GetResponseStream`。
- 原因：当前 PowerShell 返回的是新版 `HttpResponseMessage`，不能按旧版 WebResponse 的方式读取错误响应流。
- 解决方案：改用 `curl.exe -i` 读取 HTTP 状态和响应正文，避免把检查工具的类型差异误判为接口故障。
- 验证结果：待改用兼容命令复验接口返回的未授权状态。

客户端未登录接口检查复验
- 日期：2026-09-15
- 问题现象：前述 PowerShell 检查命令读取方式不兼容。
- 原因：需要使用兼容的 HTTP 客户端读取响应头和正文。
- 解决方案：已改用 `curl.exe -i` 复验。
- 验证结果：接口正常返回 HTTP 401 和 `Unauthorized`，确认是预期的登录保护，不是写操作查询接口故障。

* 登录后客户端菜谱生成返回无效工具调用
- 日期：2026-09-15
- 问题现象：登录后的客户端发送“生成一道番茄炒蛋”后未展示生成结果；对应运行约 27 秒后失败。
- 原因：千问 Agent 响应在 `QwenAgentClient` 解析阶段未形成有效的工具调用，后端返回 `AGENT_RUN_FAILED`，错误消息为“千问 Agent 返回了无效工具调用”；Redis checkpoint 显示 `round=0`、`toolCallCount=0`、`toolCalls=[]`，说明尚未进入工具执行阶段。
- 解决方案：兼容千问返回的 OpenAI 风格、直接工具调用对象和 Responses API `function_call` 结构；缺失调用 ID 时生成内部稳定 ID，参数统一规范化为 JSON 对象；当结构化调用损坏但仍有可用文本回答时降级使用文本，避免整轮误报。
- 验证结果：新增解析回归测试；后端全量测试 506/506、前端测试 159/159、前端构建通过。登录客户端重新发送生成请求后运行状态为 `COMPLETED`，已展示菜谱结果。

* 客户端恢复等待确认时操作按钮被错误禁用
- 日期：2026-09-15
- 问题现象：登录后恢复已有 `WAITING_CONFIRMATION` 运行时，小厨灵显示“等待你的确认”，但确认卡片按钮和输入框保持禁用；点击“停止”结束客户端轮询后才恢复可操作。
- 原因：`KitchenAgentWidget.trackRun` 在 `WAITING_CONFIRMATION` 分支继续轮询，但没有调用 `finishRunTracking`，导致前端 `loading` 一直为 `true`。
- 解决方案：恢复到 `WAITING_CONFIRMATION` 时立即结束客户端运行跟踪并释放 `loading`，保留确认卡片和确认按钮，不再持续轮询已暂停的运行。
- 验证结果：新增前端恢复回归测试；前端测试 159/159、生产构建通过。登录客户端恢复历史会话后确认按钮保持可用。

* 保存确认与幂等账本发生 MySQL 锁等待超时
- 日期：2026-09-15
- 问题现象：登录后确认保存菜谱，运行约 50 秒失败；后端 `AgentWriteOperationMapper.insert` 报 `Lock wait timeout exceeded`，确认仍为 `PENDING`，没有生成幂等账本记录。
- 原因：`AgentWriteService` 外层事务先更新 `agent_confirmations` 并持有行锁，随后 `AgentWriteOperationService` 的 `REQUIRES_NEW` 事务插入带 `confirmation_id` 外键的 `agent_write_operations`，形成锁反转，内层事务等待外层事务释放同一行锁。
- 解决方案：先在独立事务提交幂等账本的 `PROCESSING` 记录，再原子 claim 确认记录；确认 claim 失败时将幂等账本标记为失败并不执行业务写入。
- 验证结果：定向 AgentWriteService 测试 9/9、后端全量测试 506/506、前端测试 159/159、前端构建通过。客户端实际保存运行状态为 `COMPLETED`，确认状态为 `CONFIRMED`，幂等账本为 `COMPLETED`；使用相同幂等键再次提交返回“已完成，不会重复处理”，菜谱库仅新增 1 条记录。

前端测试命令误传 Node.js 参数
- 日期：2026-09-15
- 问题现象：本轮复验时执行 `npm test -- --run`，Node.js 报 `--run requires an argument`，测试没有按预期启动。
- 原因：项目测试脚本已经是 `node --test`，不需要再传 `--run` 参数。
- 解决方案：改用项目原有的 `npm test` 脚本重新执行。
- 验证结果：前端测试 159/159 通过，生产构建通过。

崩溃恢复演练测试首次调用 Maven 参数被 PowerShell 误解析
- 日期：2026-09-15
- 问题现象：首次执行恢复定向测试时，PowerShell 将 `-Dtest=AgentRecoveryServiceTest,InMemoryAgentRunStoreTest` 中的逗号按表达式解析，命令在进入 Maven 前报参数解析错误。
- 原因：PowerShell 下未将包含逗号的 Maven 系统属性整体作为字符串传递。
- 解决方案：将 `-Dmaven.repo.local` 和 `-Dtest` 参数都用单独的字符串参数传递后重试。
- 验证结果：恢复扫描器和内存状态仓库定向测试 6/6 通过。

客户端崩溃恢复脚本命令续行和占位符输入错误
- 日期：2026-09-15
- 问题现象：执行演练脚本时将 PowerShell 续行反引号写成 `\``，并把示例中的尖括号占位符及反斜杠一并传入参数，导致脚本收到多余参数，或使用错误的 runId/JWT 请求接口。
- 原因：PowerShell 的续行符必须是行尾单独的反引号；示例中的 `<...>` 只表示需要替换的内容，不属于实际参数值。
- 解决方案：改用单行命令，或使用不带反斜杠的反引号续行；实际参数只保留 runId 和 JWT 本身。
- 验证结果：已确认脚本参数格式，待使用进行中的真实 runId 执行 Docker 崩溃演练。

客户端在 Windows PowerShell 5.1 执行演练脚本时发生 UTF-8 编码解析错误
- 日期：2026-09-15
- 问题现象：执行 `scripts/agent-recovery-drill.ps1` 时，PowerShell 报多个“意外的标记 `}`”和“字符串缺少终止符”；脚本中的中文提示显示为乱码。
- 原因：脚本以无 BOM 的 UTF-8 保存，而 Windows PowerShell 5.1 按本地 ANSI 编码读取，中文字符串被误解码并破坏脚本语法。
- 解决方案：将演练脚本的运行时输出和错误文本改为 ASCII，避免依赖 PowerShell 的 UTF-8 自动识别；中文操作说明保留在 `agent.md`。
- 验证结果：当前环境 PowerShell 语法解析、Windows PowerShell 5.1 语法解析和 `git diff --check` 均通过；待用户使用真实 runId 和 JWT 复跑演练。

崩溃恢复演练传入已完成的 runId
- 日期：2026-09-15
- 问题现象：演练脚本返回 `The drill requires RUNNING or RECOVERING, but the current status is COMPLETED`。
- 原因：传入的 runId 已经完成，后端没有可供恢复扫描器接管的运行中任务；此时执行 SIGKILL 只能测试普通重启，不能证明 checkpoint 恢复。
- 解决方案：重新发起一个持续时间较长的 Agent 请求，拿到 `conversation.ready` 中的新 runId 后，立即在另一个 PowerShell 窗口执行演练脚本。
- 验证结果：脚本按预期拒绝已完成任务，避免把普通完成误判为崩溃恢复成功。

崩溃恢复演练重启后读取 Docker 健康状态失败
- 日期：2026-09-15
- 问题现象：后端被 SIGKILL 并重启后，脚本在轮询阶段执行 `ConvertFrom-Json` 报“传入的对象无效，应为 `:` 或 `}`”；客户端页面同时提示暂时无法读取上次运行状态。
- 原因：容器启动阶段 `docker compose ps --format json` 可能输出截断或多行 JSON，Windows PowerShell 5.1 直接解析整段输出不稳定。
- 解决方案：改用 `docker compose ps -q backend` 获取容器 ID，再用 `docker inspect --format` 读取 `State.Health.Status`，不再依赖 Compose JSON 输出。
- 验证结果：PowerShell 语法解析和 `git diff --check` 通过；重启后的 backend 已恢复为 healthy，MySQL、Redis、frontend 均保持 healthy。

崩溃恢复演练的临时恢复参数未应用到已存在容器
- 日期：2026-09-15
- 问题现象：检查重启后的 backend 环境变量时发现仍为默认的 `PT5M/PT30S`，没有使用演练脚本要求的 `PT5S/PT2S`。
- 原因：`docker compose up -d backend` 对被 kill 的已有容器只执行启动，不一定重新创建容器，因此不会刷新临时环境变量。
- 解决方案：恢复启动命令增加 `--force-recreate`，确保临时恢复参数写入新容器。
- 验证结果：脚本已更新，Compose 配置能够读取进程级的 `PT5S/PT2S` 覆盖值；待用户用新 runId 重新演练。

崩溃恢复演练已触发恢复但任务最终失败
- 日期：2026-09-15
- 问题现象：runId `174fe94e-a887-4790-9c95-9b4e16af741b` 的 backend 日志出现 `Recovering agent run ... status=RUNNING, nextNode=MODEL_DECISION`，但 Redis 最终状态为 `FAILED`，错误码为 `RECOVERY_FAILED`。
- 原因：服务重启后已从 checkpoint 继续执行；后续千问 Agent 服务调用失败，Redis 记录错误“千问 Agent 服务调用失败，请稍后重试”。当前证据不足以进一步判断是外部服务、网络还是配置问题。
- 解决方案：将“恢复触发成功”和“业务最终完成”分开判定；后续需单独检查千问服务配置、网络和调用日志。
- 验证结果：backend、frontend、MySQL、Redis 均 healthy；恢复日志存在；Redis active run 集合已移除该 runId，最终任务确实结束但未成功完成。

崩溃恢复演练脚本超时早于运行租约过期
- 日期：2026-09-16
- 问题现象：脚本在 180 秒时报告最近 10 分钟未找到恢复日志，且轮询状态仍为 `RUNNING`。
- 原因：被 SIGKILL 的原 worker 租约 TTL 为 10 分钟；恢复扫描器在租约未过期前会主动跳过该 runId，脚本的 180 秒超时早于可接管时间。
- 解决方案：继续等待租约自然过期，不删除 Redis 租约；租约释放后扫描器即可接管。
- 验证结果：租约过期后日志出现 `Recovering agent run 86e1fddb-fae9-416a-a23c-0a26dfed5a67`，Redis 最终状态为 `COMPLETED`，说明本次崩溃恢复实际成功。

合并前 Docker Compose 检查无法连接 Docker Desktop
- 日期：2026-09-16
- 问题现象：执行 `docker compose config --quiet` 和 `docker compose ps` 时提示无法连接 `dockerDesktopLinuxEngine` named pipe。
- 原因：Docker Desktop 当前未运行或 Linux Engine 尚未启动。
- 解决方案：本次只进行 Git 合并和推送，不启动或修改 Docker 服务；后续需要本地运行容器时再启动 Docker Desktop。
- 验证结果：PowerShell 脚本语法检查通过；Docker 相关检查因守护进程不可用而未完成。

步骤级恢复测试首次编译失败
- 日期：2026-09-16
- 问题现象：新增故障注入恢复测试第一次编译时，Mockito 对 MyBatis `insert` 重载调用无法判定参数类型，且等待确认测试的辅助方法参数顺序不匹配。
- 原因：`BaseMapper.insert` 同时存在单对象和集合重载，测试使用无类型 `any()` 会产生歧义；新增的确认流程构造器辅助方法需要显式传入恢复注入器。
- 解决方案：将测试桩改为 `any(AgentMessage.class)`/`any(AgentConfirmation.class)`，并补齐恢复服务辅助方法的参数。
- 验证结果：步骤级恢复定向测试 8/8 通过。

崩溃恢复脚本在恢复已触发后等待超时
- 日期：2026-09-16
- 问题现象：脚本第 5 步已找到指定 `runId` 的 `Recovering agent run` 日志，但轮询截止时任务仍为 `RUNNING`，脚本报 `The drill timed out with final status RUNNING`。
- 原因：恢复扫描已经接管任务，但恢复后的模型/工具链仍在执行；脚本把“恢复已触发”和“业务最终完成”放在同一个有限等待窗口内，超时后无法区分两者。
- 解决方案：本次先不重复执行脚本，使用运行状态接口继续轮询；后续应将恢复日志确认与最终状态等待拆开，并允许更长的恢复后处理等待时间。
- 验证结果：已确认恢复扫描日志存在；最终状态待继续轮询确认。

阶段二定向测试命令的 PowerShell 参数解析失败
- 日期：2026-09-19
- 问题现象：第一次执行 Maven 定向测试时，PowerShell 将未加引号的 `-Dtest=...` 参数中的逗号解析为表达式，报 `ParserError: Missing argument in parameter list`。
- 原因：PowerShell 对包含逗号的命令参数会进行数组/表达式解析，Maven 的 `-Dtest` 参数必须作为完整字符串传入。
- 解决方案：将 `-Dtest=AgentWriteServiceTest,AgentWriteOperationPersistenceIntegrationTest,RecipePersistenceIntegrationTest` 整体加引号后重新执行。
- 验证结果：定向测试 53/53 通过，Maven 构建成功。

阶段四 SSE 事件续传改造首次编译失败
- 日期：2026-09-19
- 问题现象：首次编译新增事件存储后，`AgentService` 找不到 `InMemoryAgentEventStore` 类型，Maven 编译失败。
- 原因：新增事件存储实现位于 `com.example.food.agent.state`，服务类只添加了接口导入，遗漏了内存实现的导入。
- 解决方案：补充 `InMemoryAgentEventStore` 导入，并保留兼容构造函数使用内存事件存储。
- 验证结果：重新编译通过，Maven BUILD SUCCESS。

阶段四 PowerShell 脚本语法检查命令首次执行失败
- 日期：2026-09-19
- 问题现象：使用嵌套 PowerShell 命令检查恢复演练脚本时，外层 PowerShell 提前展开了 `$errors`、`$null` 等变量，命令自身报 `ParserError`，未能完成脚本语法检查。
- 原因：命令字符串的引号边界不正确，变量没有作为内层 PowerShell 脚本内容保留。
- 解决方案：改用单引号包裹内层 `-Command` 脚本，避免外层变量展开后重新执行检查。
- 验证结果：改用修正命令后，`agent-recovery-drill.ps1` 与 `agent-write-crash-drill.ps1` 均解析成功；该问题不涉及项目源代码。

多食材菜谱生成漏掉指定食材
- 日期：2026-09-20
- 问题现象：输入“用鸡蛋和西红柿生成一道菜”时，模型生成的菜谱组合未覆盖鸡蛋，前端显示“AI 返回的菜谱未围绕输入食材生成（缺少：鸡蛋）”。
- 原因：生成提示词为每道菜分配了核心食材，但生成后的单道菜校验只判断是否使用任意输入食材，未判断是否使用当前菜谱槽位分配的核心食材，导致问题只能在整批结束时被发现，无法针对性重试。
- 解决方案：统一提示词与校验使用的核心食材分配；单道菜生成后校验对应核心食材，缺失时追加明确的覆盖校正指令并自动重试；保留整批覆盖校验作为最终兜底。
- 验证结果：菜谱推荐定向测试 41/41、流式生成定向测试 8/8、后端全量测试 570/570、前端测试 160/160、前端生产构建均通过。

Docker 日志检查命令首次解析失败
- 日期：2026-09-20
- 问题现象：尝试在嵌套 PowerShell 命令中使用 `2>$null` 时，外层 PowerShell 将 `$null` 提前展开，报 `Missing file specification after redirection operator`。
- 原因：嵌套命令使用双引号，内层重定向变量未被保护。
- 解决方案：去掉该重定向并拆分为独立命令，重新读取 Docker Compose 配置和后端日志。
- 验证结果：后续日志读取成功，确认当前容器仍执行旧的非流式 Agent 菜谱生成路径。

客户端真实菜谱生成测试未完成
- 日期：2026-09-20
- 问题现象：登录普通用户后发送“用鸡蛋和西红柿生成一道菜”，消息发送成功；等待约 70 秒后没有出现新的菜谱结果，界面仍停留在“小厨灵正在理解你的需求”。历史记录中可见“AI 返回的菜谱未围绕输入食材生成（缺少：鸡蛋），请点击重试”。
- 原因：当前 Docker 后端仍执行 Agent 普通生成路径，且运行容器未加载分支中尚未重新构建的修复代码；该路径没有复用流式生成路径的食材覆盖重试逻辑。
- 解决方案：在 `RecipeRecommendationService.generate()` 增加指定食材覆盖校验后的有限重试，并重新构建/重启后端镜像，再进行客户端回归。
- 验证结果：账号登录成功，客户端输入框和发送流程正常；本次请求未在观察窗口内返回最终菜谱，问题已复现。

Git 工作区统计命令首次解析失败
- 日期：2026-09-20
- 问题现象：使用嵌套 PowerShell 统计工作区改动数量时，外层命令提前展开 `$items`、`$modified` 等变量，导致 `Empty pipe element` 和 `Missing closing ')'` 解析错误。
- 原因：嵌套命令使用双引号，内层 PowerShell 变量未被保护。
- 解决方案：本次分支状态改用 Git 原生命令分别读取，不再依赖该统计命令。
- 验证结果：已成功读取当前分支、提交指向、合并状态和远程分支信息。

Docker 构建环境变量检查命令首次解析失败
- 日期：2026-09-20
- 问题现象：使用嵌套 PowerShell 查看代理环境变量时，外层命令提前展开 `$_`，报 `.Name` 找不到以及后续解析错误。
- 原因：内层 `Where-Object` 脚本块未使用受保护的引号。
- 解决方案：本次不依赖该检查结果，改为直接检查 Dockerfile、本地 Maven 缓存和 Docker BuildKit 配置。
- 验证结果：确认 Dockerfile 使用独立 BuildKit Maven 缓存，且本机 `.m2` 已存在失败日志中列出的依赖。

Docker 后端镜像构建无法下载 Maven 依赖
- 日期：2026-09-20
- 问题现象：执行 Docker Compose 构建时，`jaxb-impl:4.0.5`、`h2:2.2.224`、`spring-boot-starter-test:3.3.6` 和 `spring-security-test:6.3.5` 无法从 Maven Central 下载，报 `Remote host terminated the handshake` 和 `Read timed out`。
- 原因：Docker BuildKit 使用独立的 `/root/.m2` 缓存，未复用本机 `D:\AI-Search-food\.m2`；当前 Docker 构建网络访问 Maven Central 不稳定。
- 解决方案：使用本机 Maven 缓存进行离线构建验证；后续 Docker 构建需要恢复容器网络、配置可用镜像源，或显式复用/预热 Docker 的 Maven 缓存。
- 验证结果：`mvn -o -B -Dmaven.repo.local=D:\AI-Search-food\.m2 ... spring-boot:repackage` 已成功，说明代码和本机依赖缓存完整，故障仅发生在 Docker 构建依赖下载阶段。

Docker 镜像查询命令参数错误
- 日期：2026-09-20
- 问题现象：使用 `docker images` 同时传入多个镜像名时，Docker 报 `requires at most 1 argument`。
- 原因：`docker images` 只接受一个可选仓库参数，不能直接传入两个仓库名。
- 解决方案：改用 `docker image inspect ai-search-food-backend` 和容器 inspect 分别读取镜像与容器信息。
- 验证结果：已确认后端镜像创建时间为 2026-09-20 09:46（中国时间），容器于 09:50 启动。
Agent 普通菜谱生成路径未复用指定食材覆盖重试
- 日期：2026-09-20
- 问题现象：客户端输入“用鸡蛋和西红柿生成一道菜”时，Agent 路径偶发返回“菜谱未围绕输入食材生成（缺少：鸡蛋）”，即使流式菜谱接口已有覆盖校验，普通 Agent 入口仍可能直接失败。
- 原因：`RecipeStreamingService` 的覆盖重试只作用于流式菜谱接口；`RecipeRecommendationService.generate()` 在校验失败后没有针对缺失食材追加校正提示并重试。
- 解决方案：在普通生成入口复用指定食材覆盖校验，首次校验失败时追加“指定食材覆盖校正”提示并自动重试一次；第二次仍失败时保留原有错误返回，避免无限调用。
- 验证结果：`RecipeRecommendationServiceTest` 42/42 通过；后端全量测试 571/571 通过；Docker 后端镜像重新构建成功并健康启动。
PowerShell 健康检查命令变量被外层 Shell 提前展开
- 日期：2026-09-20
- 问题现象：检查 Docker 后端健康接口时，嵌套 PowerShell 命令中的 `$r` 被外层 PowerShell 提前展开，导致 `= : The term '=' is not recognized` 以及 `.StatusCode`、`.Content` 找不到。
- 原因：嵌套命令使用双引号传递，未保护内层 PowerShell 变量。
- 解决方案：改用单引号包裹 `-Command` 内层脚本，避免外层变量展开。
- 验证结果：本次错误发生在检查命令本身，不影响已成功构建并启动的 Docker 服务。
宿主机健康检查使用了未映射的后端容器端口
- 日期：2026-09-20
- 问题现象：直接访问宿主机 `http://localhost:7068/actuator/health` 时提示无法连接远程服务器。
- 原因：Docker Compose 只在容器网络内暴露后端 `7068` 端口，宿主机通过前端 Nginx 的 `80` 端口访问，后端端口没有映射到宿主机。
- 解决方案：改用前端健康地址 `http://localhost/healthz` 进行宿主机连通性检查；返回 `200 ok`。
- 验证结果：前端与后端容器均健康运行，容器内部健康检查也通过。
PowerShell 读取 AgentService 时误带重定向参数
- 日期：2026-09-20
- 问题现象：查看 AgentService 代码的嵌套 PowerShell 命令末尾误写了 `rtk 2>$null`，外层 PowerShell 报 `Missing file specification after redirection operator`。
- 原因：命令中不必要的重定向操作符被外层 Shell 解析。
- 解决方案：移除该重定向参数，单独执行代码读取命令。
- 验证结果：未影响源代码和运行中的 Docker 服务。

* 重复点击保存菜谱产生重复记录
- 日期：2026-09-20
- 问题现象：在小厨灵菜谱卡片上连续点击“保存菜谱”，同一道番茄炒蛋生成了多条保存记录。
- 原因：每次点击都会创建新的 Agent 保存确认，使用随机幂等键；原有幂等保护只能防止同一确认请求重复执行，不能识别同一 searchLog 的新确认。
- 解决方案：Agent 保存请求按 searchLogId 使用稳定幂等键，并处理并发唯一键冲突；落库层按用户和 searchLogId 复用已有记录；前端在保存处理中禁用按钮，保存成功后显示灰色“已保存”并禁止再次点击，失败或取消时恢复可点击。
- 验证结果：定向测试 65/65 通过；后端全量测试 573/573 通过；前端测试 161/161 通过；前端生产构建和 Docker 前端重建成功，四个容器健康运行，宿主机 `/healthz` 返回 200 ok。

并发测试辅助脚本查询事件历史返回 401
- 日期：2026-09-20
- 问题现象：并发生成测试脚本能够正常登录并完成 4 路 Agent SSE 请求，但脚本随后查询事件历史时返回 401；同一批 run 使用 PowerShell 重新登录后直接查询事件历史均返回 200，事件序号和 runId 校验通过。
- 原因：问题只出现在临时 Node.js 测试辅助脚本的后续请求链路，未能在服务端复现；服务端接口和鉴权本身未发现异常。
- 解决方案：删除临时脚本，改用独立登录后的 PowerShell 请求完成事件历史核验；后续若保留自动化脚本，需要补充 Node 请求的鉴权诊断。
- 验证结果：4 个并发 run 的事件历史均为 7 条，序号均为 1~7 连续递增，事件所属 runId 全部匹配。

并发测试账号批量注册无法取得短信验证码
- 日期：2026-09-20
- 问题现象：尝试为并发测试批量注册临时账号时，注册验证码接口返回空验证码，无法继续完成短信注册。
- 原因：Docker 配置使用阿里云短信供应商，当前本地测试环境未提供可用的短信验证码通道。
- 解决方案：不绕过短信鉴权，改用已有登录账号执行并发请求；临时确认、写操作和测试消息在测试结束后清理。
- 验证结果：未修改短信配置，也未在仓库或错误报告中记录账号密码、JWT 等敏感信息。
小厨灵语义路由修复首次编译失败
- 日期：2026-09-20
- 问题现象：执行后端定向测试时，Java 编译器提示 `SAVE_RECIPE` 在 `AgentIntentRecognizer` 中具有 private 访问权限。
- 原因：语义路由层复用了原本只供意图识别器内部使用的保存菜谱常量，但常量仍保持 private 可见性。
- 解决方案：将 `SAVE_RECIPE` 提升为公开路由常量，保持现有保存意图逻辑不变。
- 验证结果：已修复该编译错误，继续执行后端定向测试。
小厨灵语义路由状态持久化首次编译失败
- 日期：2026-09-20
- 问题现象：新增语义路由状态字段后，AgentService 的失败状态构造器参数数量与 AgentState 新结构不一致，后端编译失败。
- 原因：扩展 AgentState 后，markFailed 路径仍使用旧字段列表，没有传递语义路由尝试状态、失败状态和路由名称。
- 解决方案：在失败 checkpoint 构造时同步传递三个语义路由字段。
- 验证结果：已修复构造器参数不一致问题，继续执行后端定向测试。
小厨灵语义路由状态兼容构造器缺失
- 日期：2026-09-20
- 问题现象：后端测试编译时，使用旧版 AgentState 参数列表的测试构造失败。
- 原因：新增语义路由字段后只保留了旧的基础构造器，遗漏了“包含原有意图字段”的兼容构造器。
- 解决方案：补充原有 AgentState 构造签名，并将新增路由字段初始化为默认值。
- 验证结果：已补充兼容构造器，继续执行后端定向测试。
语义路由接入导致恢复测试失败
- 日期：2026-09-20
- 问题现象：Agent 恢复定向测试中，部分运行状态变为 FAILED，崩溃注入点未按预期触发。
- 原因：旧测试使用的意图识别 mock 没有配置新的 recognizeRoute 方法，返回 null；语义路由层直接读取结果字段导致恢复流程异常。
- 解决方案：将 null 分类结果视为不可用的语义路由结果，统一进入澄清兜底路径，不让恢复流程抛出空指针异常。
- 验证结果：已增加空结果保护，重新执行恢复测试。
* 语义路由接入导致恢复测试兼容性失败
- 日期：2026-09-20
- 问题现象：恢复测试中的旧版意图识别 mock 返回的路由结果被默认判定为不可用，导致预期的崩溃注入点未执行或运行状态提前结束。
- 原因：新路由校验依赖新增的 isUsableRoute mock 行为，旧测试替身没有配置该方法。
- 解决方案：为兼容标记为 legacy_router 的旧路由结果保留原有模型执行路径；真实分类不可用结果仍按语义失败处理。
- 验证结果：已补充兼容分支，重新执行恢复定向测试。

小厨灵“再来一道”与保存菜谱指向不一致
- 日期：2026-09-20
- 问题现象：生成第一道菜谱后点击“再来一道”，新卡片可能仍显示相同菜谱；点击保存后，确认卡片中显示的菜谱又与当前卡片不一致。
- 原因：再来一道只发送固定文本“再来一道不同的”，后端生成请求没有传入上一道菜名或排除上下文，菜谱服务会按首次生成处理，模型可能重复返回；同时前端保存按钮只发送“保存这道菜”，后端再按数据库中最新的 recipe-card 选择菜谱，而不是按用户点击的卡片选择，因此可能保存另一道菜谱。
- 解决方案：再生成时传递上一道菜谱的标识、菜名和去重上下文；保存时传递被点击菜谱的 searchLogId 或完整快照，后端按目标菜谱保存，不能再依赖“最近一条菜谱”。
- 验证结果：已通过前后端源码链路检查确认以上原因；本轮只完成定位，尚未修改功能代码。

再生成修复首次编译失败
- 日期：2026-09-20
- 问题现象：后端定向测试编译时提示 AgentService 找不到 `hasText(String)` 方法。
- 原因：新增再生成参数时误用了当前类不存在的辅助方法，项目中对应的工具方法是 Spring 的 `StringUtils.hasText`。
- 解决方案：改用已导入的 `StringUtils.hasText` 判断上一道菜名是否为空。
- 验证结果：已修正，准备重新执行后端定向测试。

小厨灵“再来一道”与保存菜谱指向修复完成
- 日期：2026-09-20
- 问题现象：点击“再来一道”可能重复第一道菜谱，点击保存后确认内容可能显示另一道菜谱。
- 原因：再生成请求缺少上一道菜谱上下文；保存请求只使用“最近一条 recipe-card”，没有绑定用户实际点击的菜谱卡片。
- 解决方案：前端再生成请求携带上一道菜名，后端传入明显不同的再生成偏好；保存请求携带当前卡片的 `searchLogId`，后端按当前会话中的目标菜谱精确查找，旧请求仍保留最近菜谱兼容兜底。
- 验证结果：后端相关定向测试 24/24、后端全量测试 576/576、前端测试 162/162 全部通过，前端生产构建成功，`git diff --check` 通过。

合并后恢复暂存改动时首次执行命令失败
- 日期：2026-09-20
- 问题现象：在 PowerShell 中执行带有 `stash@{0}` 的恢复命令时，命令解析器将花括号内容拆开，Git 报告未知选项 `e`，暂存改动未被恢复。
- 原因：PowerShell 对未加引号的 stash 引用进行了特殊解析。
- 解决方案：将 `stash@{0}` 用单引号包裹后重新执行恢复。
- 验证结果：未修改代码内容，暂存的菜谱卡片相关改动已成功恢复到 `fix/小厨灵菜谱卡片保存` 分支，`git diff --check` 通过。

语义修复推送 GitHub 失败
- 日期：2026-09-20
- 问题现象：执行 `git push origin main` 时，第一次连接被 GitHub 重置，第二次连接 GitHub 443 端口失败。
- 原因：当前环境到 GitHub 的网络连接不可用，未进入代码冲突或鉴权失败阶段。
- 解决方案：按原命令重试一次；未强制改用其他远程或绕过网络限制。
- 验证结果：本地 `main` 保留已合并的语义修复提交 `a4047c0`，远程推送尚未完成。

LLM 性能优化验证命令参数问题
- 日期：2026-09-20
- 问题现象：本次验证中首次执行后端定向测试时，PowerShell 对未加引号的逗号分隔 `-Dtest` 参数解析失败，报 `Missing argument in parameter list`；执行前端测试时误传 Jest 专用的 `--runInBand`，Node 原生测试运行器报 `node: bad option: --runInBand`。
- 原因：PowerShell 对该 Maven 参数的逗号语法进行了错误解析；前端项目使用 `node --test`，不支持 `--runInBand`。
- 解决方案：将 Maven `-Dtest` 参数整体加引号；按照 `frontend/package.json` 的项目脚本直接运行前端测试。
- 验证结果：后端定向测试 86/86、性能改动定向测试 92/92、后端全量测试 578/578、前端测试 162/162 均通过；参数问题未改动源代码。
菜谱生成校验失败且重试后重复展示
- 日期：2026-09-20
- 问题现象：输入“生成一道黄瓜和火腿的菜”时，Agent 提示 AI 返回的菜谱缺少“黄瓜”；点击重试后，页面同时保留原失败消息和新的相同输入记录，顶部还显示旧会话的“等待你的确认”状态。
- 原因：模型返回的 ingredients 数组未完整覆盖用户指定食材，后端食材覆盖校验拒绝了结果；前端重试逻辑会追加新消息，不会替换原失败消息；页面恢复时还会显示数据库中尚未清理的旧运行状态。该现象不能证明检索判定黄瓜和火腿无法搭配。
- 解决方案：强化菜谱提示词，要求每个指定食材逐项写入 ingredients 并在 steps 中使用；双食材检索增加“黄瓜 火腿 家常做法”组合查询；保留严格校验并记录 requested/generated/missing 便于定位。验证时重启后端并清空一次旧会话，避免历史失败记录干扰。
- 验证结果：菜谱相关定向测试 51/51 通过，后端全量测试 579/579 通过；前端重试记录去重尚未修改，若需优化需另行确认前端交互方案。

Docker 真实 Agent 恢复演练未观测到恢复日志
- 日期：2026-09-21
- 问题现象：真实杀掉后端容器并以 `AGENT_RECOVERY_ENABLED=true`、`AGENT_RECOVERY_STALE_AFTER=PT5S`、`AGENT_RECOVERY_SCAN_DELAY=PT2S` 重启后，后端恢复健康，但目标 run 在脚本窗口内持续为 `RUNNING`，最近日志没有 `Recovering agent run`，恢复演练脚本按设计判定失败。
- 原因：本次运行中的后端实例没有产出恢复扫描日志；需继续核对当前 Docker 镜像是否已包含仓库中的恢复实现，以及启动后的调度扫描是否实际生效。
- 解决方案：保留该 run 和容器现场，先不清理 Redis/MySQL 数据，准备基于当前源码重建后端镜像后重新执行同一故障窗口。
- 验证结果：后端容器、MySQL、Redis 均恢复健康；业务运行状态仍为 `RUNNING`，本轮不能记为恢复成功。

演练账号 BCrypt 哈希生成辅助命令首次失败
- 日期：2026-09-21
- 问题现象：第一次用 JShell 调用项目 BCrypt 编码器时，因未带完整 Spring 日志依赖而报 `NoClassDefFoundError: org/apache/commons/logging/LogFactory`。
- 原因：临时校验命令只加载了 `spring-security-crypto`，没有同时加载 `spring-jcl` 等运行依赖。
- 解决方案：补齐项目本地 BCrypt 运行依赖后重新生成哈希；未修改项目源码。
- 验证结果：专用演练账号密码登录成功，取得有效用户令牌，继续执行真实崩溃演练。

Docker 写事务崩溃演练结果核验首次使用错误表名
- 日期：2026-09-21
- 问题现象：写事务崩溃演练脚本已经通过，但追加核验“演练食材未写入库存”时，查询不存在的 `pantry_items` 表，MySQL 返回 `ERROR 1146`。
- 原因：项目实际库存表名为 `user_pantry_items`，核验命令沿用了错误的表名。
- 解决方案：改用 `user_pantry_items` 重新查询，没有删除或修改业务数据。
- 验证结果：幂等账本仍为 `PROCESSING`、确认仍为 `PENDING`，演练食材记录数为 0；四个 Docker 服务均 healthy。

Docker 真实崩溃演练准备阶段数据库命令首次登录失败
- 日期：2026-09-21
- 问题现象：创建真实崩溃演练专用账号时，第一次通过 PowerShell 外层双引号拼接容器内 MySQL 命令，MySQL 返回 `Access denied for user 'root'@'localhost'`。
- 原因：外层 PowerShell 对容器命令中的环境变量和转义符进行了提前解析，导致容器内的数据库密码变量没有按预期传递。
- 解决方案：改用 PowerShell 单引号完整包裹容器内命令，使 `$MYSQL_ROOT_PASSWORD` 和 `$MYSQL_DATABASE` 留在容器内解析；未修改应用代码或数据库配置。
- 验证结果：容器内 `SELECT 1` 成功，演练专用账号创建并核验成功，后续真实崩溃演练继续进行。

Agent 恢复接管租约改动首次编译失败
- 日期：2026-09-21
- 问题现象：新增 Redis 租约接管逻辑后，Maven 编译提示找不到 `RedisScript` 类型。
- 原因：首次使用了错误的 Spring Data Redis 包路径。
- 解决方案：改为 `org.springframework.data.redis.core.script.RedisScript`。
- 验证结果：恢复相关定向测试 16/16 通过。

* Docker 恢复租约在旧实例崩溃后阻塞新实例接管
- 日期：2026-09-21
- 问题现象：第二次真实 Docker 演练中，旧实例在被 SIGKILL 前恰好拿到 `recovery:` 租约；新实例检测到恢复租约后不再接管，任务长期保持 `RUNNING`，演练超时。
- 原因：仅按租约 owner 前缀保护恢复任务，无法区分仍存活的恢复实例和已崩溃实例。
- 解决方案：为恢复实例增加短期存活标记；同一恢复实例的租约继续受保护，旧实例标记过期后允许新实例原子接管，并为旧格式 owner 保留兼容接管路径。
- 验证结果：更新镜像后对 run `6c26de13-873e-4298-b0fc-c83d15e4f461` 执行 SIGKILL 演练，出现恢复日志并最终 `COMPLETED`，脚本通过。

AgentRecoveryProperties 配置绑定失败
- 日期：2026-09-21
- 问题现象：第一次将扫描间隔加入 `@ConfigurationProperties` record 并增加重载构造函数后，Docker 后端启动失败，提示 `No default constructor found`。
- 原因：Spring Boot 对该 record 的多构造函数绑定无法按预期实例化。
- 解决方案：恢复原有三字段配置绑定，恢复实例标记 TTL 使用实现内固定的短窗口，不改变现有配置结构。
- 验证结果：后端重新启动为 healthy，相关后端测试 20/20 通过，Docker 恢复演练随后通过。

千问 Agent Docker 演练遇到外部模型限流
- 日期：2026-09-21
- 问题现象：一次稳定运行态准备请求返回 HTTP 429 `Too many requests`，Agent 进入 `FAILED`，无法作为崩溃恢复演练样本。
- 原因：千问模型服务端限流，属于外部依赖运行异常。
- 解决方案：保留任务失败记录，不将其误判为恢复代码失败；降低测试请求频率并切换到可用模型配置后重新执行。
- 验证结果：后续 Docker 演练成功完成，恢复任务最终为 `COMPLETED`。

* 前端测试命令误传不兼容参数
- 日期：2026-09-21
- 问题现象：首次执行前端测试时附带 `--runInBand`，Node 原生测试运行器报 `node: bad option: --runInBand`。
- 原因：该项目使用 `node --test`，不是 Jest，不能使用 Jest 专用参数。
- 解决方案：改用项目原生 `npm test` 命令。
- 验证结果：前端测试 164/164 通过，生产构建通过。

本轮 Docker 可观测接口核验命令首次调用方式不兼容
- 日期：2026-09-21
- 问题现象：读取验证记录时先后直接以 `rtk sed` 和 `rtk Get-Content` 调用，运行时提示找不到对应可执行程序。
- 原因：当前终端通过 `rtk` 转发外部命令，不能直接把 PowerShell 内置命令或 Unix 工具当作可执行程序传入。
- 解决方案：改用 `rtk pwsh -NoProfile -Command` 执行 PowerShell 读取命令，未修改项目代码或运行数据。
- 验证结果：错误报告读取成功；随后管理员登录和 Agent 可观测接口实测成功，返回恢复配置及实时指标。

本轮浏览器验证等待方法首次调用错误
- 日期：2026-09-21
- 问题现象：首次浏览器页面核验调用了不存在的 tab 级 `waitForTimeout` 方法，页面未受影响。
- 原因：等待方法属于页面 Playwright 对象，应通过 `tab.playwright.waitForTimeout` 调用。
- 解决方案：改用页面 Playwright 等待方法后继续验证，未修改业务数据。
- 验证结果：管理员登录成功，Agent 可观测面板可从侧边栏打开，指标卡、图表、质量比率和恢复配置均正常展示。

本轮 Docker 快照核验 SQL 引号错误
- 日期：2026-09-21
- 问题现象：首次读取 `agent_metric_snapshots` 时，空值占位符的引号被 PowerShell/容器命令解析为列名，MySQL 返回 `Unknown column 'NONE'`。
- 原因：验证命令的多层引号转义不正确，不是应用或数据库迁移问题。
- 解决方案：改用不带空值占位符的 `COUNT(*)` 与 `MAX(captured_at)` 查询，未修改业务数据。
- 验证结果：MySQL 快照表成功回读，随后确认历史记录从 1 条增长至 6 条。

* 自动评测集首次编译缺少类型导入
- 日期：2026-09-21
- 问题现象：执行自动评测集针对性测试时，Maven 编译提示 `AgentEvaluationService` 无法解析 `AgentToolRegistry`。
- 原因：新增评测服务漏写 `AgentToolRegistry` 的 import。
- 解决方案：补充正确的类型导入，未改变评测逻辑。
- 验证结果：已进入重新编译与测试流程。

* 自动评测集测试类同样缺少类型导入
- 日期：2026-09-21
- 问题现象：修正生产类后，Maven 在 testCompile 阶段继续提示评测测试类无法解析 `AgentToolRegistry`。
- 原因：测试类独立使用该类型，也遗漏了 import。
- 解决方案：补充测试类导入，未改变测试断言。
- 验证结果：准备重新执行针对性测试。

真实 Docker 恢复演练启动脚本请求头参数拆分
- 日期：2026-09-21
- 问题现象：启动真实 Agent 流式任务时，curl 将带空格的 Authorization 和 Accept 请求头拆分为主机参数，导致请求返回 `curl: (6) Could not resolve host`，未进入 backend 崩溃演练阶段。
- 原因：Windows PowerShell `Start-Process -ArgumentList` 未保留数组元素内部的空格边界。
- 解决方案：改用临时 curl 配置文件传递请求头、请求体和 URL，演练结束后自动删除临时文件。
- 验证结果：修复后重新执行真实 Docker 恢复演练。

真实 Docker 恢复演练临时请求体路径转义
- 日期：2026-09-21
- 问题现象：curl 能读取临时配置文件，但无法打开配置中引用的 Windows 请求体路径，路径反斜杠被解析为转义字符，未进入 backend 崩溃演练阶段。
- 原因：curl 配置文件中的 `data-binary` 路径没有转换为跨平台的正斜杠格式。
- 解决方案：写入 curl 配置前将临时文件路径转换为正斜杠，保留配置文件传参方式。
- 验证结果：修复后重新执行真实 Docker 恢复演练。

真实 Docker 恢复演练默认提示词触发食材覆盖校验
- 日期：2026-09-21
- 问题现象：backend 被 SIGKILL 后成功接管活动 run，但恢复任务最终为 `FAILED/RECOVERY_FAILED`。
- 原因：默认提示词没有明确列出食材，被路由识别为单个食材名称；恢复后的菜谱覆盖校验缺少该输入项。
- 解决方案：将演练默认提示词改为明确包含鸡蛋、番茄和葱花的晚餐菜谱请求；恢复逻辑和租约流程无需修改。
- 验证结果：修正提示词后重新演练通过，目标 run 最终为 `COMPLETED`。

* 用户管理表格固定操作列继续遮挡最后登录字段
- 日期：2026-09-21
- 问题现象：第一次将操作列改为不透明固定列后，字段虽然不再透出，但固定列仍覆盖了未横向滚动到视口内的“最后登录”列，页面看起来仍像字段重复或被挡住。
- 原因：固定列本身会脱离表格横向滚动流，在当前视口宽度不足时必然覆盖右侧字段。
- 解决方案：移除操作列的 `fixed="right"`，让操作列与其他列一起参与横向滚动，避免任何列互相遮挡。
- 验证结果：重新构建 Docker 前端并通过用户管理页面视觉回归后，字段按列独立显示，操作列仅在横向滚动到对应位置时出现。

* 用户管理操作列窄视口下按钮文字贴近裁切边界
- 日期：2026-09-21
- 问题现象：取消固定列后，窄视口横向滚动到操作区域时，“禁用”按钮右侧文字贴近表格边界，视觉上像被挡住。
- 原因：操作列最小宽度偏大，操作按钮进入可视区域时没有足够的紧凑空间。
- 解决方案：将操作列最小宽度从 190px 调整为 160px，保留“禁用/启用”和“解锁/无需解锁”的完整间距，并继续使用横向滚动而不覆盖时间字段。
- 验证结果：前端测试、生产构建和 Docker 页面回归通过；横向滚动到操作区域后，按钮文字完整显示。

Docker 管理员验收首次使用历史默认密码失败
- 日期：2026-09-22
- 问题现象：执行管理员可观测面板、自动评测集和 Actuator 指标的真实 Docker 验收时，使用文档中的历史默认密码登录返回 `Invalid admin credentials`。
- 原因：当前持久化 MySQL 中的管理员密码已不同于迁移脚本里的历史默认值，属于本地运行数据与初始化文档不一致。
- 解决方案：未修改业务代码或数据库结构，改用当前本地管理员已配置的凭据完成验收；不在报告中记录密码。
- 验证结果：管理员接口、6 个评测用例、可观测面板和 8 个 Actuator 指标均通过真实 Docker 请求验证。

2026-09-22 第一阶段记忆定向测试参数解析失败
- 问题现象：第一次执行包含多个测试类的 Maven 定向测试时，PowerShell 报参数列表解析错误，测试没有启动。
- 原因：包含逗号的 `-Dtest` 参数没有整体加引号，被 PowerShell 按表达式解析。
- 解决方案：将完整的 `-Dtest=...` 参数整体加引号后重新执行。
- 验证结果：记忆模块定向测试随后正常启动并通过。

2026-09-22 第一阶段记忆服务 Spring 上下文启动失败
- 问题现象：记忆持久化集成测试启动 Spring 上下文时，提示记忆服务没有默认构造器。
- 原因：服务同时提供生产环境构造器和测试构造器，Spring 无法自动判断注入入口。
- 解决方案：为生产环境构造器增加 `@Autowired`，并保留测试需要的构造方式。
- 验证结果：记忆模块定向测试和后续后端全量测试均通过。

2026-09-22 第一阶段记忆唯一索引断言不稳定
- 问题现象：H2 集成测试中，记忆事件表的唯一索引已生效，但按预期索引名查询不到。
- 原因：使用表级唯一约束时，H2 会生成内部索引名称，无法保证与 MySQL 中的约束名称一致。
- 解决方案：改为显式创建命名唯一索引，保持 MySQL 与 H2 的索引名称一致。
- 验证结果：Flyway 37 个迁移成功执行，记忆模块 7 项定向测试和后端全量 594 项测试全部通过。

2026-09-22 行为入口盘点命令使用了不兼容的 PowerShell 写法
- 问题现象：盘点测试和源码入口时，命令中使用 Unix `head` 以及 PowerShell 不支持的路径通配写法，分别出现找不到命令和路径语法错误；未修改项目文件。
- 原因：当前终端是 PowerShell，不能直接使用 Unix 管道工具和 Unix 风格的目录通配参数。
- 解决方案：改用 `rg` 的原生筛选能力，并对每个目标文件使用明确路径读取。
- 验证结果：现有搜索、收藏、烹饪、反馈、冰箱和成品评价入口均完成盘点。

2026-09-22 烹饪 Episode 载荷空值导致定向测试失败
- 问题现象：行为接入定向测试中，烹饪消耗测试因新增 Episode 载荷组装抛出 NullPointerException，35 项测试中 1 项错误。
- 原因：使用 `Map.of` 构造载荷，而历史菜谱允许标题为空；`Map.of` 不允许任何 value 为 null。
- 解决方案：将烹饪消耗载荷改为允许空字段的有序 Map，保持原烹饪和库存扣减流程不变。
- 验证结果：已完成修复，等待重新执行行为接入定向测试确认。
- 补充验证结果：修复后行为接入定向测试 35 项全部通过。

2026-09-23 手动验收发现冰箱直接操作未接入 Episode
- 问题现象：通过浏览器登录后，在“我的食材”页面执行“添加食材”和“消耗库存”，页面操作均成功，库存数量也正确变化；但页面的库存变动记录没有显示新增记录。
- 原因：当前页面调用的是 `UserPantryService.create/consume` 直接库存接口，第二阶段仅在 `PantryOperationService` 的 stock-in、cooking consume、undo 入口接入 `MemoryBehaviorEpisodeRecorder`，因此页面常用的直接冰箱入口未产生 Episode。
- 处理建议：下一轮修复应将 `UserPantryService` 的新增、合并数量和直接消耗入口统一接入 Episode，并补充浏览器验收与 Episode 持久化查询验证。
- 验证结果：搜索、收藏、烹饪、评分等依赖 AI 菜谱的页面流程因本地 `.env` 未配置 AI API Key 无法完成生成；登录、冰箱新增和冰箱消耗页面操作已完成，其中冰箱 Episode 接入验收未通过。

2026-09-23 冰箱直接 Episode 回归测试断言首次失败
- 问题现象：新增的直接冰箱 Episode 单元测试首次执行失败，测试期望食材名称被归一化为中文，但实际结果仍为输入的 `tomato`。
- 原因：现有 IngredientNormalizer 对该英文名称没有映射为中文 canonical name，新增测试错误假设了归一化结果。
- 解决方案：将测试断言调整为当前项目真实行为 `tomato`，不修改业务逻辑。
- 验证结果：UserPantryServiceTest 15/15 通过，后续 UserPantryPersistenceIntegrationTest 4/4 通过。

2026-09-23 后端重启后浏览器测试账号失效
- 问题现象：为加载修复代码重启使用 H2 内存数据库的后端后，原浏览器登录态访问冰箱页面时提示“登录状态已失效”。
- 原因：H2 内存数据库随应用进程重启清空，原手动验收账号和登录数据不再存在。
- 解决方案：使用新的本地合成测试账号重新登录，未使用真实账号或真实敏感数据。
- 验证结果：重新登录后完成冰箱页面添加土豆 2、消耗 1 的人工操作，页面库存从 2 变为 1。

2026-09-23 菜谱人工流程因 API Key 缺失暂停
- 问题现象：人工输入“鸡胸肉、西兰花、鸡蛋”提交菜谱搜索后，页面显示“生成未完成，AI API Key 未配置，请先在管理后台设置”，因此尚未进入收藏、烹饪和评分步骤。
- 原因：当前本地环境未配置菜谱生成所需的 AI API Key。
- 处理方案：已将浏览器停在管理员登录入口，等待用户登录管理后台并自行填写、保存 API Key；助手不读取、不代填、不记录密钥。
- 验证结果：待用户完成配置后继续人工复测完整流程。

2026-09-23 菜谱保存人工复测被管理员身份拦截
- 问题现象：API Key 配置后，管理员会话重新生成 3 道菜谱成功；点击“保存到我的菜谱”时被跳转到普通用户登录页，并提示“请先登录，再保存到我的菜谱”。
- 原因：菜谱保存属于普通用户资源操作，管理员身份不能直接保存到个人菜谱库。
- 处理方案：保留当前流程结果，等待用户登录普通用户账号后重新执行搜索并继续保存、烹饪、评分。
- 验证结果：API Key 已生效，菜谱生成成功；保存、烹饪和评分尚待普通用户会话复测。

2026-09-23 之前本地测试账号手机号无法从脱敏列表恢复
- 问题现象：管理员用户管理页面显示两条本地测试用户记录，但手机号中间部分被脱敏；根据可见前缀和后缀尝试登录时均提示“手机号尚未注册”。
- 原因：后台出于安全设计只返回脱敏手机号，之前测试账号的完整手机号和密码未被助手保存。
- 处理方案：停止猜测并清空登录表单，等待用户在本地页面输入正确手机号，通过验证码登录；未记录或输出任何凭据。
- 验证结果：尚未进入普通用户会话，菜谱保存、烹饪和评分流程继续等待登录。

* 2026-09-23 第二阶段人工验收连接到未升级的本地数据库
- 问题现象：前端搜索、收藏、烹饪、反馈、冰箱操作和成品菜评价可以完成，但查询 `memory_episodes` 时提示表不存在，无法确认任何 Episode 是否落库。
- 原因：浏览器连接的本地后端使用的 MySQL 迁移历史停在 V14；Docker MySQL 迁移历史停在 V36。当前代码需要 V37 创建 `memory_episodes`，因此这两套运行数据库都不是当前第二阶段可验收的数据库状态。
- 处理方案：经用户确认后先备份本地数据库，再重启当前分支后端，让 Flyway 应用缺失迁移；未迁移 Docker 数据。
- 验证结果：定向测试 35/35、全量测试 599/599 通过；Flyway 成功应用 23 个迁移至 V37。数据库核对到搜索、收藏、反馈、烹饪、入库、扣减、撤销和成品评价 Episode 共 10 条，字段、JSON、状态及幂等键检查通过；删除收藏待用户在操作时确认，双账号人工隔离未执行。迁移期间 V26 的三个 `IF NOT EXISTS` 表产生非致命已存在警告，后端仍成功启动。

2026-09-23 验收诊断命令参数兼容问题
- 问题现象：诊断过程中数条 PowerShell/RTK、Docker 模板和 MySQL 命令因参数转义或模板字段写法不兼容而执行失败；未修改数据库结构。
- 原因：在 Windows PowerShell 环境中混用了 cmdlet、RTK 包装和 shell 引号规则；信息架构字段应为 `TABLE_SCHEMA`，而非 `SCHEMA_NAME`。
- 解决方案：改用 RTK 代理的 PowerShell 脚本及本地 MySQL 客户端，并修正信息架构列名。
- 验证结果：诊断确认 Docker 数据库保持 V36；本地 MySQL 随后已由当前分支后端成功升级到 V37，并确认记忆表存在。

2026-09-23 后端环境与本地配置诊断输出凭据
- 问题现象：诊断时误将后端容器环境变量和本地数据源配置中的凭据输出到工具结果。
- 原因：读取配置时未先过滤敏感变量或配置项。
- 处理方案：停止读取或输出密钥值；建议轮换本地 AI、云访问、JWT、Redis、短信及本地 MySQL 相关凭据。本次未将凭据值写入文件、错误报告或提交。
- 验证结果：后续查询仅输出迁移状态和业务数据，不再读取凭据值。

2026-09-23 成品评价记录查询使用了不存在的状态字段
- 问题现象：只读核对成品评价记录时，SQL 使用了 `finished_dish_reviews.status`，MySQL 返回未知列错误。
- 原因：成品评价表不包含 `status` 字段。
- 解决方案：按实际表结构查询 `id`、`user_id`、`recipe_id`、`ai_model` 和 `created_at`。
- 验证结果：确认当前测试用户有一条与本次菜谱关联的成品评价记录；Episode 载荷中的 reviewId 与该记录对应。

2026-09-23 Docker V37 迁移验收查询命令引号解析失败
- 问题现象：首次在 PowerShell 中嵌套执行 Docker MySQL 版本查询时，容器 shell 报 `unexpected EOF while looking for matching \"`，未读取到查询结果。
- 原因：外层 PowerShell、RTK 和容器内 shell 同时解析 SQL 引号，转义层级不正确。
- 解决方案：改用直接执行 `rtk docker exec ... sh -c` 的方式查询，避免额外的 PowerShell 嵌套转义。
- 验证结果：随后成功确认 Docker Flyway 已从 V36 升级到 V37；数据库未因该命令失败而产生额外修改。

2026-09-23 Docker 人工验收过程中的操作命令与页面索引错误
- 问题现象：一次 Docker SQL 查询误用了不存在的单数表名 `recommendation_feedback`；一次浏览器操作复用了已失效的页面元素索引；首次写入测试用户密码时 shell 展开了哈希中的 `$` 字符。
- 原因：表名未先按当前数据库结构确认；页面异步更新后旧的无障碍索引失效；容器 shell 对双引号中的 `$` 进行变量替换。
- 解决方案：改用实际表名 `recommendation_feedbacks`，重新获取页面状态后再操作，并对哈希中的 `$` 做 shell 转义后重新写入。
- 验证结果：测试用户登录成功，菜谱搜索、收藏、喜欢反馈和完成烹饪均成功；Docker 数据库 Episode 与反馈记录核对通过。

2026-09-23 记忆候选阶段首次编译失败
- 问题现象：新增候选记忆模块首次编译时失败，`MemoryBehaviorEpisodeRecorder` 的重载构造器对 `null` 解析不明确，`JsonNode` 版本不支持 `valueStream()`。
- 原因：新增可选候选服务后保留了两个三参数构造器，测试构造器传入裸 `null`；项目当前 Jackson 版本的 `JsonNode` 没有使用中的流式方法。
- 解决方案：对 `null` 显式转换为 `MemoryCandidateService`，并改用 `JsonNode.forEach` 收集数组文本。
- 验证结果：已完成修复，准备重新执行定向测试。

2026-09-23 记忆候选服务首次启动失败
- 问题现象：定向集成测试的 Flyway 已成功升级到 V38，但 Spring 容器启动失败，提示 `MemoryCandidateService` 没有默认构造器。
- 原因：该服务同时保留了生产构造器和带测试时钟的包级构造器，Spring 未能自动选择注入构造器；可选服务提供器在解析候选服务时触发了实例化失败。
- 解决方案：为生产构造器增加 `@Autowired`，明确 Spring 的依赖注入入口。
- 验证结果：已完成修复，准备重新执行定向测试。

2026-09-23 记忆候选自动提取测试首次编译失败
- 问题现象：新增录制器自动提取测试时，测试编译器提示找不到 Mockito 的 `when` 方法。
- 原因：测试增加了带返回值的 Mock 配置，但未补充对应的静态导入。
- 解决方案：补充 `when` 的静态导入。
- 验证结果：已完成修复，准备重新执行定向测试。

2026-09-23 Docker 人工验收发现候选记忆未落库
- 问题现象：搜索、收藏、喜欢和标记已做均能在页面完成，`memory_episodes` 已生成对应 Episode，但 `memory_candidates` 与 `memory_evidence` 没有记录。
- 原因：候选提取挂在业务事务提交回调中，使用原事务上下文执行，无法保证提交后的候选写入拥有独立的提交边界；同时候选服务使用可选提供器注入，生产链路可能被静默跳过。
- 解决方案：改为直接注入候选服务，并增加提交后使用 `REQUIRES_NEW` 独立事务的提取入口。
- 验证结果：代码修复后的候选录制、提取和持久化定向测试 8/8 通过；等待 Docker 重建后重新人工复测。

2026-09-23 候选事务修复首次编译失败
- 问题现象：调整录制器构造器后，Maven 编译提示三参数构造器重复并发生递归构造调用。
- 原因：将原本不同参数类型的可选提供器构造器改为候选服务后，与包级测试构造器签名重复。
- 解决方案：删除重复的公开构造器，保留带 `@Autowired` 的包级生产构造器和测试用的双参数构造器。
- 验证结果：候选记忆定向测试 8/8 通过，BUILD SUCCESS。

2026-09-23 菜谱烹饪完成弹窗层级遮挡
- 问题现象：人工点击“完成烹饪”后，库存确认弹窗已生成，但被“烹饪步骤模式”遮挡，无法直接看到“仅标记完成”和“完成并扣减库存”。
- 原因：两个前端对话框同时保留，步骤模式的层级覆盖了库存确认弹窗。
- 处理方案：人工关闭步骤模式后完成“仅标记完成”，确认业务流程可继续；前端弹窗层级仍需后续修复。
- 验证结果：关闭步骤模式后库存确认可操作，烹饪流程成功完成；该问题不影响 Episode 写入，但当前人工验收不能视为完整无缺陷通过。

2026-09-23 Docker Desktop 引擎无法重建后端
- 问题现象：重建后端时 Docker API 无法连接；Docker Desktop 日志显示 `sailor-ingest.sock` 临时 socket 无法访问，backend 引擎启动失败。
- 原因：Docker Desktop 的临时运行 socket 处于 Windows 不可访问的 reparse/socket 状态，Docker 内部 WSL 终止后仍无法移动或删除该运行文件。
- 处理方案：仅重启本次启动的 Docker 进程并终止 `docker-desktop` WSL；未删除数据库容器、镜像、卷或项目数据。待 Docker Desktop 修复或系统重启后再重建后端。
- 验证结果：Java 定向测试 8/8、全量测试 605/605 通过；修复后的 Docker 人工复测和候选表复核因 Docker 引擎未恢复暂未完成。

* 2026-09-23 Docker 候选记忆修复后的人工复测
- 问题现象：人工点击“完成烹饪”后，库存确认弹窗仍被“烹饪步骤模式”遮挡，必须先关闭步骤模式才能操作“仅标记完成”；成品评价测试图与黑椒鸡胸肉菜谱不匹配，系统按业务规则返回 0 分并提示无法有效评价。
- 原因：烹饪步骤弹窗和库存确认弹窗同时保留且层级覆盖；测试图片实际展示鸡蛋，不是当前菜谱成品。
- 处理方案：关闭步骤模式后完成人工流程；重新上传有效图片并完成成品评价，未修改业务数据绕过不匹配结果。
- 验证结果：Docker 服务健康，Flyway 已升级到 V38；搜索、保存、喜欢、烹饪完成和成品评价 Episode 均落库；同一 Episode 的候选提取键无重复，候选与 Evidence 已落库，相关错误日志为空。烹饪弹窗层级问题仍需单独修复。

2026-09-23 记忆合并服务首次编译失败
- 问题现象：新增记忆合并服务后，Maven 编译提示 Lambda 捕获的局部变量必须是 final 或 effectively final。
- 原因：候选流式过滤逻辑中对同一个局部变量先查询后重新赋值，再在 Lambda 中引用。
- 解决方案：抽取未处理候选查询为独立的命令式辅助方法，避免 Lambda 捕获被重新赋值的局部变量。
- 验证结果：编译成功；记忆合并定向测试 9/9 通过；全量测试 606/606 通过。

2026-09-23 写操作超时修复首次编译失败
- 问题现象：新增写操作处理中状态超时判断后，Maven 编译提示找不到 `Duration` 类型。
- 原因：新增 `Duration` 常量时遗漏了 `java.time.Duration` 导入。
- 解决方案：补充 `Duration` 导入后重新编译验证。
- 验证结果：等待定向测试重新执行。

2026-09-23 旧菜谱保存操作卡在处理中
- 问题现象：人工回归中，历史“番茄炒蛋”保存确认一直显示“保存中”，对应 `agent_write_operations` 长时间保持 `PROCESSING`，后续重试无法判断是否已经写入。
- 原因：浏览器断开流式连接后，持久化写操作账本和确认单没有超时回收；同时确认单的过期校验早于幂等账本检查，导致旧操作直接返回过期。
- 解决方案：增加 20 分钟超时回收，将无法确认结果的写操作和确认单标记为 `UNKNOWN_REVIEW`，并在过期校验前先检查对应幂等操作；不自动猜测业务写入结果，避免重复保存。
- 验证结果：定向测试 65/65 通过；Docker 人工点击历史确认后显示人工复核提示，数据库中操作和确认单均变为 `UNKNOWN_REVIEW`，没有新增重复菜谱。

2026-09-23 浏览器中断助手流式响应产生 Broken pipe
- 问题现象：人工测试期间离开仍在回放的助手页面时，后端日志出现 `Broken pipe` 以及 text/event-stream 写出异常。
- 原因：浏览器主动断开 SSE 连接后，服务端仍尝试写入已关闭的响应；异常处理器随后无法再向流式响应写入普通错误响应。
- 处理方案：本次先将其作为客户端主动中断的可观测运行噪声记录，未将其当作业务写入失败；后续应将 SSE 客户端断开作为正常结束处理并避免二次写响应。
- 验证结果：受控人工确认流程中未出现数据重复或业务失败；该日志处理仍是后续可优化项。

2026-09-23 烹饪确认弹窗层级问题已修复
- 问题现象：人工点击“完成烹饪”后，库存确认弹窗与烹饪步骤弹窗同时存在，库存确认按钮无法稳定操作。
- 原因：父级烹饪步骤弹窗没有在打开库存确认前关闭，两个弹窗形成了重叠的模态层。
- 解决方案：打开库存确认前关闭步骤弹窗；用户选择“返回烹饪”时再恢复步骤弹窗，完成操作后保持关闭。
- 验证结果：前端测试 167/167、生产构建通过；Docker 人工回归时只显示库存确认弹窗，“仅标记完成”可操作且两个弹窗均正常关闭。

2026-09-23 标签归一化迁移首次测试失败
- 问题现象：新增 V40 标签归一化迁移后，H2 集成测试启动失败，提示一次 `ALTER TABLE` 添加多列的 SQL 语法错误。
- 原因：MySQL 接受的批量加列写法不被项目测试使用的 H2 2.2 兼容。
- 解决方案：将 memory_candidates 和 memory_items 的字段变更拆成逐条 `ALTER TABLE ... ADD COLUMN`，保留 MySQL 与 H2 的共同语法。
- 验证结果：Flyway 40 个迁移全部成功，标签归一化定向测试 5/5 通过，后端全量测试 612/612 通过，Docker 数据库实际升级到 V40。

2026-09-23 前端回归命令参数误用
- 问题现象：一次前端测试命令使用了项目不支持的 `--run` 参数，Node 报告 `--run requires an argument`。
- 原因：将其他测试运行器的参数套用到项目实际使用的 `node --test` 脚本。
- 解决方案：改用项目原生 `npm test` 命令重新执行。
- 验证结果：前端测试 167/167 通过；该问题是测试命令调用错误，不是产品功能失败。

2026-09-24 记忆检索与上下文阶段初次自动化验证问题
- 问题现象：新集成测试先后出现测试数据构造参数不匹配、Spring 无法选择带多个构造器的 Bean，以及上下文画像筛选用例未通过。
- 原因：测试 Session 命令遗漏 conversationId；检索 Bean 的生产构造器未显式标注注入；画像筛选把元数据字段数量当成有效画像判断，并将“菜谱历史查询”误归为推荐意图。
- 解决方案：修正测试数据参数、明确 Spring 构造器注入；画像仅在任务相关分组存在时加入上下文，并将历史查询和推荐任务分别判定。
- 验证结果：阶段定向测试通过；后端全量测试 621/621 通过。

2026-09-24 Docker Desktop 引擎状态检查不可用
- 问题现象：检查 Compose 服务状态时，Docker CLI 无法连接 `dockerDesktopLinuxEngine` 命名管道。
- 原因：本机 Docker Desktop Linux 引擎当时未提供连接；该问题发生在本地验证环境，不是应用功能故障。
- 解决方案：本轮不操作容器、卷或用户服务；使用项目 H2/MySQL 兼容集成测试完成数据库查询验证。
- 验证结果：后端全量测试 621/621 通过；Docker Compose 容器状态未能核实，需在 Docker 引擎恢复后再检查。

2026-09-24 Agent记忆上下文定向测试首次失败
- 问题现象：记忆上下文提供器测试预期 Session 只更新一次，定向测试报告一次调用预期与两次实际调用不符。
- 原因：Session 先记录检索阶段的记忆 ID，再记录经上下文预算筛选后实际注入模型的记忆 ID，因此设计上需要两次乐观锁更新。
- 解决方案：将测试断言修正为验证两次更新，并保留对检索与最终使用 ID 的分别记录。
- 验证结果：记忆上下文提供器、Agent 接入和 Qwen 双协议定向测试共 21/21 通过，BUILD SUCCESS。

2026-09-24 Agent 自由文本菜谱无法保存
- 问题现象：对 Agent 说“生成一道红烧牛肉”，得到完整菜谱文本并被询问是否保存；回复“需要”后，系统提示“当前没有可保存的菜谱记录”。
- 原因：从现象初步判断，该轮只生成了文本，未形成保存操作所需的可收藏菜谱记录；但助手仍提示可以保存，行为前后不一致。尚未验证结构化菜谱卡片的保存入口。
- 解决方案：待排查该自由生成路径；若不创建可收藏记录，应避免提示可保存；若产品预期可保存，则需在生成时创建并传递有效菜谱记录标识。
- 验证结果：该交互的保存闭环未通过；尚未修复。下一步需通过结构化菜谱卡片重测，以区分问题范围。

2026-09-24 人工测试账号与目标账号不一致
- 问题现象：人工测试时浏览器实际登录为 user_id=7，而目标手机号对应 user_id=1；测试搜索并保存的验收菜谱“番茄炒蛋”因此写入 user_id=7。
- 原因：执行写入操作前未先用当前登录身份与目标手机号对应的 user_id 核对，测试账号选择错误。
- 解决方案：暂停该会话后续写入测试；待浏览器切换至 user_id=1 并核实身份后继续。新增的 user_id=7 测试收藏暂时保留，不擅自删除。
- 验证结果：MySQL 查询确认菜谱记录 id=147 属于 user_id=7；user_id=1 的近期 Episode、画像、候选与 Evidence 未被这次菜谱写入改变。目标账号人工验收仍未完成。

2026-09-24 Agent 菜谱卡与本轮指定菜名不一致
- 问题现象：输入“生成一道红烧牛肉”时，助手正文描述红烧牛肉，但可保存菜谱卡曾返回包含六道菜的晚餐组合；若按该卡片保存会收藏错误菜谱。
- 原因：模型工具参数和历史上下文中的食材/菜名可能覆盖本轮明确要求；单道菜请求仍允许使用库存组合规划，且原先没有校验生成标题是否符合指定菜名。
- 解决方案：从本轮消息提取明确菜名并优先于工具参数；点名生成时不复用旧食材或库存、不运行多菜规划；把目标菜名加入提示词，结果标题不符时重试一次，仍不符则拒绝生成卡片。
- 验证结果：新增定向回归覆盖旧参数覆盖、库存隔离与错名重试；定向测试 53/53、后端全量 629/629、前端测试 167/167、前端构建成功。目标账号浏览器复测得到“清淡版红烧牛肉”卡，正文和卡片均对应红烧牛肉；确认保存后在“我的菜谱”核实记录 #148。历史确认卡未操作。

2026-09-24 Agent 模型请求超时后重试成功
- 问题现象：目标账号首次发送红烧牛肉请求后，界面提示 Agent 调用失败。
- 原因：后端日志显示 DashScope 兼容接口请求触发 SocketTimeoutException；本地服务与数据库健康，未发现本地后端故障。
- 解决方案：对同一未完成请求仅重试一次，没有修改超时配置或反复消耗模型调用。
- 验证结果：重试成功返回与红烧牛肉相符的菜谱正文和结构化卡片，随后保存闭环成功。模型接口偶发超时仍可能再次发生。

2026-09-24 菜名提示模板格式参数遗漏
- 问题现象：新增目标菜名提示后，首次定向测试中多项菜谱生成用例因 MissingFormatArgumentException 失败。
- 原因：规划提示模板增加了格式占位符，但对应参数没有在同一模板的格式参数列表中同步增加。
- 解决方案：为规划提示补齐目标菜名参数；明确点名的单道菜跳过多菜规划，并将目标名加入实际生成提示。
- 验证结果：Agent 与菜谱服务定向测试 53/53 通过，后端全量测试 629/629 通过。

2026-09-24 浏览器验收辅助接口误用
- 问题现象：一次状态等待调用使用了当前 CUA 标签页不支持的 getByText 方法，未触发页面操作。
- 原因：混用了 Playwright locator 与 CUA 标签页接口。
- 解决方案：改用 CUA 可访问性树观察页面状态。
- 验证结果：后续已正常完成菜谱生成、保存确认及收藏列表核实；页面和数据无损。

2026-09-24 手动回归中的辅助工具调用异常
- 问题现象：验收过程中有数次辅助检查命令因 RTK 不支持 PowerShell 原生命令、Windows shell 引号解析和 SQL 引号写法不兼容而失败；一次可访问性节点在界面刷新后失效，另一次关闭按钮检查未命中；尝试在受限脚本环境中解码诊断文本时也因缺少内置解码器失败。
- 原因：把不同 shell/浏览器接口的语法混用，并在状态刷新后沿用旧节点编号；部分诊断辅助 API 不可用。
- 解决方案：改用 RTK 文件读取和单项搜索命令，修正只读 SQL 查询；每次操作后重新读取可访问性树，确认界面状态；未对这些失败重复执行有副作用的操作。
- 验证结果：应用数据无损，页面操作最终状态均经重新观察确认；Docker 服务保持健康。未解码的辅助诊断字段没有用于验收结论。

2026-09-24 Agent 记忆召回在用户可见追踪面板中缺少明细
- 问题现象：询问“根据之前的饮食行为，我有哪些已确认的长期饮食偏好”时，Agent 正确返回与账号记忆相符的菜谱偏好；数据库中的该轮会话记录了所选 memory item 与 Episode ID，但页面展开“本次使用了哪些数据”只显示“小厨灵正在理解你的需求”，未展示记忆检索/使用阶段。
- 原因：本次验收确认后端完成检索、构建上下文并记录了所选记忆；用户可见追踪内容没有呈现这些记忆阶段与来源。具体是 SSE 追踪事件还是前端映射缺失，尚未通过代码变更验证。
- 解决方案：待后续补齐或确认 Agent 记忆阶段的用户可见追踪，让检索到与实际注入的记忆来源可解释。
- 验证结果：只读数据库核对了该轮会话的 selected memory IDs；回答提及的红烧牛肉偏好有两条明确反馈证据，相关近期食材偏好也有对应候选记录。核心召回通过，追踪展示仍待改进。

* 2026-09-24 Agent 记忆召回追踪缺少明细并误标近期行为
- 问题现象：页面只显示通用的“正在理解你的需求”，没有展示本轮实际使用的记忆；补上明细后，近期行为推断一度被标成“长期偏好”。第一次定向测试还发现未配置记忆提供器时对空结果读取状态导致空指针。
- 原因：Agent 只向 SSE 发出通用阶段事件，没有发送记忆检索结果；追踪摘要格式化时未使用记忆 temporalType；测试覆盖的无记忆提供器兼容路径没有考虑空值。
- 解决方案：在记忆检索前后发送 SSE 阶段与安全摘要，仅展示实际纳入上下文的条目，不展示内部 ID 或原始载荷；按 LONG_TERM/RECENT 区分长期偏好与近期行为推断；对记忆提供器未启用的情形安全降级。
- 验证结果：AgentService 与记忆上下文定向测试 10/10 通过；后端全量测试 629/629、前端测试 167/167、前端生产构建通过；Codex 内置浏览器实际查询后，展开追踪可看到“长期偏好”和“近期行为推断”分别列出，页面回答也区分明确反馈与近期行为。Docker 后端重建成功；未删除或更改用户菜谱、库存等业务数据。

2026-09-24 手动冰箱测试产生临时库存记录
- 问题现象：为验证冰箱直接入库/消耗及菜谱采购入库/撤销流程，测试账号新增了“验收测试菠菜”1g并消耗至0g；另将菜谱“清淡版红烧牛肉”的牛腩模拟入库1g后成功撤销，库存恢复为0g。
- 原因：需要在浏览器中覆盖实际库存操作及反向操作闭环。
- 解决方案：采购入库仅使用1g，并立即通过“撤销库存变动”恢复；未修改原有食材。两个测试食材的0g记录因未执行删除而保留。
- 验证结果：页面分别显示采购入库“已撤销”和反向变动“已完成”；数据库新增对应 PANTRY_STOCK_IN、PANTRY_CONSUMED 与 PANTRY_OPERATION_REVERSED Episode，均属于当前 user_id=1。临时0g食材记录仍在账号库存中。

2026-09-24 跨账号隔离人工验收受测试账号状态阻塞
- 问题现象：用户提供的测试号码提交注册验证码请求后，页面提示“该手机号已经注册”；应用在发短信前拒绝了重复注册，因此未发送短信，也未创建或修改账号。
- 原因：该号码已存在于用户表；当前 Docker 后端运行 mysql、prod、docker 配置，短信提供方为阿里云 PNVS，未注册号码的验证码会发送真实短信。
- 解决方案：未尝试重置密码或绕过注册校验；请用户在浏览器中自行登录已有测试账号后继续隔离验收，或提供一个未注册且本人可接收验证码的号码并确认发送一次，亦可先将本地环境切换为模拟短信。
- 验证结果：合成红烧牛肉图片的成品评价通过，页面返回综合分85且历史评价中可见记录；跨账号隔离仍待完成。照片测试评价保留在当前账号中，未执行不可恢复的删除。

2026-09-24 全量人工回归中 AI 服务请求失败
- 问题现象：AI 菜谱生成两次均提示流式服务调用失败；Agent 记忆召回验收请求提示“千问 Agent 服务调用失败，请稍后重试”；食材识别上传合成测试图并启动后，等待约 15 秒仍返回“等待识别”；成品图片评价上传并提交后，等待约 20 秒仍回到“等待成品图”，没有生成评分或历史记录。
- 原因：本轮 Agent 请求的 Docker 后端日志确认 DashScope 兼容接口 TLS 握手失败（`SSLHandshakeException`，远端关闭连接）；菜谱生成、食材识别及成品评价没有找到对应异常日志，不能确认它们与同一握手故障有关。
- 解决方案：本轮未修改应用或 AI 配置；需先确认本机 Docker 到 DashScope 的 TLS/网络连通性，并补查菜谱流式与图像识别请求的服务端日志后再判断是否需要代码修复。
- 验证结果：后端和前端自动化测试均通过，但以上四项实时 AI 功能未通过本轮人工验收；浏览器控制台没有 JavaScript error，也未观察到成品评价历史新增。无密钥 TLS 探测在 Windows 主机与 Docker 后端容器中均以握手失败结束，确认是当前环境到 DashScope 的网络/TLS 连通性问题。

2026-09-24 冰箱消耗弹窗默认数量未写入表单
- 问题现象：库存消耗弹窗显示最小值 0.01，但直接确认会报“请输入大于 0 的消耗数量”；手动将数量输入为 0.10 后消耗成功。
- 原因：打开弹窗时 `consumeForm.quantity` 被设为 `null`，控件显示的最小值没有同步到表单模型，验证器因此收到空值。
- 解决方案：本轮未修改代码；建议让表单模型在弹窗打开时与控件默认值一致，并补充“未碰数量控件直接提交”的组件测试。测试期间已将库存数量恢复到原值。
- 验证结果：确认了默认提交失败、输入有效数量后成功；该交互缺陷尚未修复。

2026-09-24 冰箱手动消耗未显示在最近操作列表
- 问题现象：手动消耗库存成功并刷新后，库存数量正确变化，但“库存变动记录 / 最近操作”仍没有对应的手动消耗记录。
- 原因：前端手动消耗调用直接扣减库存的 `/users/me/pantry/{id}/consume` 接口，成功后只更新库存列表；与库存操作列表使用的操作接口及刷新流程不同。
- 解决方案：本轮未修改代码；需确认产品是否要求手动消耗也进入可撤销操作流水，再统一后端记账和前端刷新行为。
- 验证结果：消耗 0.10 后数量减少 0.10，随后已将库存恢复到原值；操作列表未出现本次手动消耗。

2026-09-24 DashScope TLS 连通性恢复及实时 AI 人工复测
- 问题现象：上一轮主机与 Docker 后端访问 DashScope 均出现 TLS 握手中断，导致 Agent、菜谱生成及图片功能人工验收未完成。
- 原因：本轮未找到代理配置或证书配置异常；在未修改网络/应用配置后，TLS 连接恢复，原始握手中断应为暂时性网络故障，具体外部原因无法确认。
- 解决方案：没有关闭证书校验、修改代理或更换密钥；通过主机和 Docker 后端的无凭据 HTTPS 探测确认 TLS 恢复，再从应用执行人工复测。
- 验证结果：主机与容器均获得 TLS 校验成功的 HTTP 404 根路径响应；真实菜谱生成 3/3、Agent 记忆召回及来源追踪、食材识别（牛肉/土豆/葱）与识别结果交接、合成成品照片评价（85 分并写入历史）均通过。应用和密钥未改动。

* 2026-09-24 冰箱手动消耗默认值与操作流水问题修复
- 问题现象：消耗弹窗显示 0.01，但不触碰数量控件就提交会被校验拒绝；手动消耗后库存减少，但“最近操作”没有对应记录。
- 原因：弹窗数量模型为 null；手动消耗只更新库存并写 Episode，没有写 pantry_operations，也没有刷新操作列表。
- 解决方案：默认数量初始化为可用库存与 0.01 的较小值；手动消耗改由库存操作服务事务化记账，保存扣减前后数量与幂等键；重复键不会重复扣减，且不能用于不同请求；成功后刷新库存和操作列表，并支持现有撤销流程。
- 验证结果：定向后端测试 35/35、全量后端测试 633/633、前端测试 167/167，前端生产构建成功；更新后的 Docker 后端健康。浏览器人工复测按默认 0.01kg 提交成功，最近操作出现“手动消耗”；撤销后库存恢复至原 2kg，原操作标为“已撤销”、反向操作标为“已完成”；浏览器控制台无错误。测试产生的消耗/撤销历史保留，未删除数据库记录或更改原有库存。

* 2026-09-24 Agent 记忆召回后的菜谱生成输入校验
- 问题现象：浏览器中请求结合“番茄黄瓜炒鸡丁”的历史反馈推荐晚餐时，记忆已实际召回并展示来源，但菜谱生成因将“推荐一道适合我的晚餐”“避免番茄黄瓜炒鸡丁的风味组合”识别为必需食材而拒绝结果。
- 原因：Agent 菜谱工具在模型未提供明确食材时，会把自然语言请求整体回退为食材输入；同一实现也存在于当前 main，属于既有菜谱参数解析问题，不是本次长期记忆代码的改动。
- 解决方案：在独立修复分支中，只从用户本轮原始消息解析明确食材；自然语言意图、口味说明和记忆内容不作为硬性食材约束；未提供食材时启用 AI 自主选材，并明确工具参数约束。
- 验证结果：定向测试 17/17、全量后端测试 636/636 通过。Docker 后端已重建并健康；浏览器重复原失败请求后成功生成“青椒炒牛肉”，未再把请求说明当食材，来源面板显示本轮召回的长期记忆和 Episode。

* 2026-09-24 Personalized Skill 测试夹具 JSON 转义错误
- 问题现象：PersonalizedSkillServiceTest 的画像值清洗用例失败，预期个性化策略包含清洗后的食材名称，实际画像解析为空。
- 原因：Java 文本块中的 `\n` 被解释成 JSON 字符串内部的真实换行，导致测试画像 JSON 无效，服务按设计降级为空画像。
- 解决方案：将测试数据中的换行写成 JSON 转义序列 `\\n`，让 JSON 解析后再由画像名称清洗逻辑移除控制字符。
- 验证结果：PersonalizedSkillServiceTest、ContextBuilderTest、AgentMemoryContextProviderTest、AgentServiceTest 定向测试共 21 项全部通过，BUILD SUCCESS。

2026-09-24 Personalized Skill 追踪摘要测试断言重复
- 问题现象：调整来源面板摘要顺序后，AgentMemoryContextProviderTest 首次定向测试失败，期望列表中个性化策略摘要出现两次，实际实现只输出一次。
- 原因：为确保策略摘要不会被记忆摘要数量上限截断，将其前置后，测试仍保留了旧位置上的重复预期。
- 解决方案：删除重复的测试预期，保留策略摘要位于首位且只展示一次的断言。
- 验证结果：记忆模块定向测试 21/21、后端全量测试 642/642 通过；Docker 后端健康，浏览器实际生成推荐并在来源面板显示“已加载本轮任务对应的个性化执行策略”。

2026-09-25 高影响饮食目标确认闭环定向测试初次失败
- 问题现象：新增集成测试首次编译缺少 Spring Boot 测试注解导入及 JSON 序列化异常声明；修正后，Mapper XML 解析和一条个性化策略文案断言仍导致定向测试失败。
- 原因：测试夹具漏导入 `SpringBootTest` 并未声明受检异常；普通 Mapper 注解 SQL 与 `<script>` SQL 对 XML 运算符的解析不同；策略测试断言的子串与完整指令顺序不一致。
- 解决方案：补齐测试导入和异常声明；普通注解 SQL 使用兼容的 `!=`，动态 XML SQL 使用 `&lt;&gt;`；将策略断言聚焦于画像中应生效/不应生效的目标值。
- 验证结果：记忆确认相关定向测试 21/21 通过；后端全量测试 649/649 通过，BUILD SUCCESS。

2026-09-25 记忆接口普通用户权限测试夹具映射未加载
- 问题现象：补充记忆接口权限测试后，匿名访问和管理员访问分别按预期返回 401、403；普通用户请求测试地址返回 404。
- 原因：测试切片未加载临时测试控制器上的新增映射，因此该断言没有经过实际记忆控制器路由。
- 解决方案：在 MVC 测试切片中加载真实 `MemoryConfirmationController` 并 Mock 其应用服务，再验证普通用户可以访问、管理员不可访问。
- 验证结果：`SecurityConfigTest` 17/17 通过。
2026-09-25 README 截图整理脚本首次执行失败
- 问题现象：首次批量重命名截图的代理命令未执行；README 写入脚本因运行环境缺少 TextEncoder 提前终止；一次中英文局部补丁因补丁格式不兼容失败。
- 原因：代理 shell 提前展开了 PowerShell 命令变量；当前脚本环境未提供 TextEncoder；补丁接口不接受未带行号的更新块。
- 解决方案：改用显式路径整理截图、手动 UTF-8 编码写入 README，并通过 PowerShell 文本替换更新英文提示。
- 验证结果：四张截图均已重命名并与 README 引用一致；中英文 README 均成功写入，文档和图片链接存在，`git diff --check` 无错误。

* 2026-09-25 README 技术栈补丁格式调整
- 问题现象：编辑中英文 README 时，一次多段补丁因上下文行缺少格式标记而被拒绝，目标文件未被修改。
- 原因：补丁中的上下文行未添加所需前缀。
- 解决方案：拆分为较小补丁，分别更新中英文标题、技术徽章和技术栈表格。
- 验证结果：目标内容已更新；`git diff --check -- README.md README.en.md` 通过；技术版本已对照项目配置核对。
2026-09-25 README 截图哈希校验命令不可用
- 问题现象：校验遮挡后的截图文件时，当前 PowerShell 代理环境无法识别 `Get-FileHash`。
- 原因：该代理环境未提供 `Get-FileHash` 命令。
- 解决方案：改用 .NET 内置 SHA-256 实现校验。
- 验证结果：临时预览与 `docs/images/readme/main-scene.png` 的 SHA-256 一致；图片尺寸为 2544×1174。

2026-09-25 记忆管理浏览器验收测试账号初始化异常
- 问题现象：隔离测试账号首次密码登录失败，后端提示存储密码不是有效 BCrypt；浏览器同时将通过终端初始化的中文测试昵称和记忆实体显示为乱码。
- 原因：测试账号初始化 SQL 经 Shell 传递时展开了 BCrypt 字符串中的 `$`；终端传递中文测试数据时编码不一致。这是测试夹具问题，不是应用代码故障。
- 解决方案：仅修复隔离测试账号的密码字段时将 BCrypt 值按十六进制传入，并将该账号昵称及合成记忆实体改为 ASCII 测试文本；未触及其他账号。
- 验证结果：BCrypt 长度恢复为 60 且失败计数清零；随后通过本机浏览器成功登录隔离测试账号并完成记忆查看、编辑、刷新、单条删除及清空确认弹窗检查。

2026-09-25 记忆管理 API 本机测试地址连接失败
- 问题现象：Node 测试脚本访问 `http://localhost` 的本机 API 时连接失败，浏览器页面访问正常。
- 原因：该 Node 运行时通过 `localhost` 未命中 Docker 映射端口；具体地址解析细节未进一步确认。
- 解决方案：本机测试请求改用 IPv4 回环地址 `127.0.0.1`。
- 验证结果：登录及个性化设置关闭/开启 API 调用成功，浏览器刷新后分别正确显示关闭和开启状态；最终恢复为开启。

2026-09-25 长期记忆评测构建与集成测试问题
- 问题现象：首次编译 `AgentMemoryContextProvider` 时，成功路径返回值引用了未声明的 `prepared`；修复后，H2 集成测试又因未检测到命名唯一索引而失败。
- 原因：新增追踪写入后没有先将构建结果绑定到局部变量；迁移使用唯一约束生成索引，测试按显式索引名检查，数据库元数据名称不一致。
- 解决方案：保存 `PreparedContext` 局部变量后记录追踪并返回；迁移改为创建显式命名的唯一索引。
- 验证结果：定向测试 19/19 通过；后端全量测试 661/661 通过，BUILD SUCCESS；Docker MySQL 成功应用 V43。

* 2026-09-25 Docker Agent 调用 DashScope 的 TLS 握手短暂失败
- 问题现象：首次浏览器实时 Agent 请求失败，后端记录远端关闭 TLS 握手，未能返回回答。
- 原因：本轮未确认具体根因；随后无密钥 HTTPS 探测通过证书校验并收到 HTTP 400，说明 TLS 连接恢复，但不能据此断定网络抖动的来源。
- 解决方案：没有更改密钥、证书校验或网络配置；连通性恢复后使用隔离测试账号重试。
- 验证结果：浏览器获得正常 Agent 回复；页面显示当前账号没有可加入上下文的长期记忆，数据库追踪为 SUCCESS，用户 9 的长期记忆条目和 Episode 数均为 0。

2026-09-25 长期记忆评测样本构造编译失败
- 问题现象：首次定向测试编译 `MemoryEvaluationService` 时，两个过期记忆样本未提供记录要求的 `staleId` 参数。
- 原因：新增 `StaleFixture` 时只传入了候选集合，遗漏用于统计 Top-3 过期记忆命中的样本 ID。
- 解决方案：在两个样本中补齐对应过期记忆 ID，并保持统计口径为“进入 Top-3 的过期样本数 / 过期候选数”。
- 验证结果：定向接口测试 6/6 通过；后端全量测试 661/661 通过，BUILD SUCCESS。

2026-09-25 记忆反馈数据库约束断言不匹配
- 问题现象：新增记忆反馈定向测试首次运行时，迁移已成功创建 `memory_feedback`，但测试将唯一约束按索引目录查询，导致一项 Flyway 元数据断言失败。
- 原因：H2 将该命名唯一约束记录在 `INFORMATION_SCHEMA.TABLE_CONSTRAINTS`，而不是测试所查询的 `INFORMATION_SCHEMA.INDEXES`。
- 解决方案：保持 V44 命名唯一约束不变，将测试改为直接验证唯一约束名称及类型。
- 验证结果：记忆反馈相关定向测试 22/22 通过；后端全量测试 672/672 通过，BUILD SUCCESS。

* 2026-09-25 个性化回复与记忆证据表述矛盾
- 问题现象：普通用户请求“结合我之前的饮食偏好”后，来源面板列出“近期行为推断：喜欢生姜”，但回复称“完全省略生姜（基于你近期反馈）”，将正向证据表述成了负向反馈。
- 原因：浏览器来源面板和代码中的记忆摘要都按画像的正负极性展示了正向“喜欢生姜”，说明本次检索/来源追踪链路没有反转极性。`AgentService` 注入模型的约束只泛化要求区分长期/近期、显式/推断并说明依据；没有规定负向结论必须有 `DISLIKE` 或明确拒绝证据，也没有对最终回答做记忆证据一致性校验。模型因此把正向的近期行为推断误写成用户近期反馈并据此声称省略生姜。
- 解决方案：补充 Agent 提示约束，明确 LIKE/DISLIKE 极性、显式表达与行为推断的区分，并禁止用当前未召回到的记忆推断完整历史；新增最终回复确定性校验，仅在本轮加载个人记忆/画像时生效。若回复把正向证据说成用户负面反馈或据此声称避开食材，则改为引用当前正向记忆的说明；若同时存在同一食材的负向证据，则保留冲突交由模型结合时间和范围解释。画像摘要和检索命中摘要均纳入识别，并增加对应单元测试。
- 验证结果：定向测试 16/16 通过；后端全量测试 682/682 通过，BUILD SUCCESS。Docker 后端重建后健康，数据库保持 V44，未改动 MySQL/Redis 数据卷。浏览器用当前登录账号再次询问生姜偏好，回复限定为“当前记忆中没有找到明确表态”，并正确说明近期行为推断为可能偏好生姜；没有再次将正向证据描述为负面反馈，也没有作“从未表达”的全历史断言。本次模型回复自然符合约束，运行日志未出现确定性改写告警；确定性改写路径由单元测试覆盖。

2026-09-26 记忆冲突测试断言编译失败
- 问题现象：首次定向测试编译 MemoryConflictResolverTest 时，AssertJ 的 `.first()` 链式结果类型不支持 `.contains(...)`，测试代码无法编译。
- 原因：断言链 API 类型推导与预期字符串断言不匹配。
- 解决方案：将列表数量断言与对首项字符串内容的断言拆开。
- 验证结果：记忆冲突相关定向测试 15/15 通过；后端全量测试 691/691 通过，BUILD SUCCESS。

2026-09-26 Docker 记忆冲突人工测试数据编码异常
- 问题现象：临时写入隔离测试画像时，MySQL 终端连接字符集将中文食材名保存为问号，导致合成记忆无法用于浏览器验收。
- 原因：该交互式 MySQL 会话使用 latin1 连接字符集，直接传入的中文字面量被替换。
- 解决方案：改用 UTF-8 十六进制字节并显式按 utf8mb4 转换；随后恢复测试前画像内容，并按专用幂等键和 consolidation key 删除本次两条合成 Episode 与两条合成 Memory Item。
- 验证结果：画像 JSON 有效且长度恢复为 9455 字节，用户记忆条数为 25，本次合成 Episode 和 Memory Item 均为 0；浏览器人工交互在发送测试消息前被用户中断。

* 2026-09-26 Agent 对含旧错误回答的会话未稳定遵从记忆时间线
- 问题现象：复测同一历史会话时，模型仍把“反馈清除、再次点不喜欢、成品评分”的顺序解释成时间线矛盾；切到干净测试对话后，模型按事件时间正确复述顺序，并能按当前场景解释正反向记忆。
- 原因：旧会话中的模型错误回答会作为聊天历史继续进入推理，可能与新召回的记忆证据竞争；此前上下文也没有足够醒目地呈现事件时间和先后关系。
- 解决方案：为 Episode 上下文增加完整发生时间并按时间排序；冲突上下文显式列出正反向证据的事件时间、场景和优先依据；Agent 提示要求保留另一方向的历史，不得称其被永久覆盖。旧会话保留不动，使用新会话验证修复后的行为。
- 验证结果：定向测试 13/13 通过、后端全量测试 693/693 通过；浏览器干净测试对话验证了“较早的训练后喜欢”优先于“较新的日常不喜欢”，检索追踪状态 SUCCESS 且包含 MEMORY_CONFLICTS。测试后已恢复测试账号画像偏好数组及两个 Episode 的原始时间/载荷，未改动长期记忆记录。

2026-09-26 Episode 时间顺序测试断言修正
- 问题现象：新增上下文测试首次断言一个并未展示在提示上下文中的 Episode 类型名称，导致测试失败。
- 原因：测试期望与 ContextBuilder 实际输出字段不一致；上下文展示的是事件摘要、载荷和发生时间，不展示该类型标识。
- 解决方案：改为断言清除、不喜欢、成品评分三个事件的完整时间戳按先后顺序出现。
- 验证结果：定向测试 13/13 通过；后端全量测试 693/693 通过，BUILD SUCCESS。

2026-09-26 时间线防护测试夹具时间戳设置错误
- 问题现象：扩展 Agent 记忆上下文测试后，定向测试首次有 1 项失败，实际历史行为摘要使用运行时当前时间，与测试预期的固定时间不一致。
- 原因：固定时间戳误设置在 MEMORY_ITEM 命中，而需要固定时间的是 EPISODE 命中。
- 解决方案：恢复画像命中的动态时间，并将 Episode 命中设为固定时间，避免测试随运行日期变化。
- 验证结果：定向测试 24/24 通过；后端全量测试 695/695 通过，BUILD SUCCESS。Docker 后端重建后健康；浏览器复测明确区分了有序事件与正反偏好冲突。
